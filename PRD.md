# MyAPP 产品需求文档（PRD）

- 版本：v1.3
- 日期：2026-08-05
- 平台：Android 原生（单人自用，不上架）
- 目标设备：**realme GT Neo5 SE**（骁龙 7+ Gen 2 / 144Hz 1.5K AMOLED / **realme UI 7.0 · Android 16**），minSdk 35 · targetSdk 36
- 核心约束：**无自建服务器**，所有数据本地存储，仅直连公开网页与 RSS
- 设计基调：Claude 风格——简洁、优雅、暖色调，配以克制而高级的动效

**版本变更记录**

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿：8 个功能模块、本地优先架构、可扩展性设计 |
| v1.1 | 飞书接入由 OAuth 改为**公开网页**；新增**预算**功能与每笔支出即时提示；新增**桌面小组件**；新增 UI 设计规范与动效方案两章 |
| v1.2 | 确定目标设备为 realme GT Neo5 SE，新增设备适配章节 |
| v1.3 | 设备系统确认为 **Android 16**，版本基线提至 minSdk 35 / targetSdk 36；动效底座接入 **Material 3 Expressive**；补充骨架落地情况 |

---

## 1. 产品定位

一个「个人生活 + 知识 + 信息」聚合的私人 App。把日常记录（纪念日/经期/待办/记账）、个人知识库（飞书公开页）、行业信息（RSS）收在一个入口里，随手记、随手查。

**设计原则**

| 原则 | 含义 |
|---|---|
| 本地优先 | 所有个人数据落在本机 Room 数据库，断网可用；网络只用于网页抓取和 RSS 拉取 |
| 无服务器 | 不做任何后端。知识库走公开网页直连；新闻走端上 RSS 抓取解析 |
| 流畅 | 冷启动 < 800ms，144Hz 下滚动不掉帧，所有 IO 在协程后台，UI 只消费 Flow |
| 单人自用 | 不做多用户、不做权限体系、不做账号系统；安全边界 = 手机本身 |

**非目标（明确不做）**

- 不做多设备实时同步（V1 只提供手动导出/导入备份）
- 不做社交、分享、协作
- 不做云端账号、不做数据上传到自有服务

---

## 2. 用户与场景

唯一用户：你本人。

典型场景：
1. 早上打开 App → 首页看到「今日待办 + 日程提醒 + 对象经期预测 + 今天的知识点卡片 + 3 条行业新闻」。
2. 想到一件事 → 2 秒内记一条「疑问」或「笔记」，不打断当前事情。
3. 收到微信/支付宝支付通知 → App 后台自动记一笔账，并立刻告诉你本月预算还剩多少。
4. 不打开 App，扫一眼桌面小组件就知道今天花了多少、还有什么没做。
5. 周末回顾 → 看本周花销、看未解决的疑问列表、翻知识库。

---

## 3. 功能范围

### 3.1 模块总览

| # | 模块 | 优先级 | 依赖 |
|---|---|---|---|
| M1 | 纪念日 & 经期记录 | P0 | 本地 |
| M2 | 待办与日程提醒 | P0 | 本地 + 通知 |
| M3 | 快速笔记 | P0 | 本地 |
| M4 | 疑问收集箱 | P0 | 本地 |
| M5 | 记账（通知栏自动解析 + 预算） | P1 | 通知监听权限 |
| M6 | 飞书公开网页收藏与阅读 | P1 | 网络（无需授权） |
| M7 | 知识点每日推送 | P1 | 依赖 M6 |
| M8 | 行业新闻（自配置 RSS） | P2 | 网络 |
| M9 | 桌面小组件 | P1 | 依赖 M2/M5 |
| M0 | 首页聚合 Dashboard | P0 | 依赖以上 |

---

### 3.2 M1 纪念日 & 经期记录

**经期记录**
- 记录每次月经的开始日期、结束日期（可留空表示进行中）、备注（痛经/量/情绪）。
- 自动计算：平均周期长度（近 6 次）、平均经期天数、下次预计开始日、当前处于第几天 / 距下次还有几天。
- 首页卡片显示：`距下次约 5 天` 或 `经期第 3 天`。
- 提醒：预计开始日前 N 天（默认 2 天，可配置）本地通知；经期中每日可选提醒关怀。
- 日历视图：月历上用色块标出历史经期区间 + 预测区间（预测用浅色/虚线区分）。
- 预测算法（V1 简单可靠）：`下次开始 = 最近一次开始 + round(近 6 次周期均值)`。样本 < 2 次时用默认 28 天，并在 UI 明确标注「样本不足，仅供参考」——不要给出看起来很确定但其实没依据的数字。

**纪念日**
- 字段：标题、日期、类型（公历/农历）、重复（一次性/每年）、提前提醒天数、备注。
- 列表按「距今天数」升序，显示 `还有 12 天` / `已过去 300 天`。
- 支持累计型纪念日（如「在一起第 X 天」）。
- 需支持农历（生日常用农历）——用本地农历换算库，不联网。

**验收标准**
- 新增一条经期记录 ≤ 3 次点击完成。
- 关机重启后提醒仍能准时触发（精确闹钟 + 开机广播重建）。

---

### 3.3 M2 待办与日程提醒

- 字段：标题、备注、截止时间（可空）、优先级（高/中/低）、标签、重复规则（每天/每周/每月/自定义间隔）、完成状态。
- 视图：今日、最近 7 天、全部、已完成。
- 完成后重复任务自动生成下一条。
- 提醒：到期时间点本地通知；支持「稍后提醒（15 分钟 / 1 小时 / 明天）」。
- 首页「今日待办」区块显示未完成项，可直接勾选完成。
- **逾期项高亮并置顶**——过期待办若沉在列表底部就等于不存在了。

**权限**：`POST_NOTIFICATIONS` 运行时申请；精确闹钟用 `USE_EXACT_ALARM`（安装即授予，见 9.2）。

---

### 3.4 M3 快速笔记

- 首页悬浮按钮 / 通知栏常驻快捷入口 → 直接进输入框，聚焦即可打字。
- 字段：正文（Markdown 轻语法：标题、列表、加粗、代码块）、标签、创建/更新时间、是否置顶。
- 支持插入图片（Photo Picker 选图，存本地私有目录，数据库存相对路径）。
- 全文搜索（Room FTS），搜索结果高亮命中词。

---

### 3.5 M4 疑问收集箱

定位：与笔记分开——笔记是「已知的沉淀」，疑问是「未解的待办知识」。

- 字段：疑问内容、来源上下文（可空）、标签、状态（待解决 / 已解决 / 已归档）、答案正文（解决时填写）。
- 列表默认按状态分组，`待解决` 优先。
- 解决后可一键「转为笔记」，把疑问 + 答案合成一条笔记存入 M3。
- 首页可选显示「随机一条待解决疑问」，起提醒作用。

---

### 3.6 M5 记账（通知栏自动解析 + 预算）

> 这是「不知如何实现」那条需求的落地方案。安卓上无服务器实现自动记账，唯一可靠路径是 **NotificationListenerService 监听支付类通知并解析**。

#### 3.6.1 自动记账

**工作流程**
1. 设置页开启「自动记账」→ 跳转系统「通知使用权」页面授权。
2. App 后台常驻 `NotificationListenerService`，只处理白名单包名：微信、支付宝、云闪付、各银行 App。
3. 命中白名单后取 `EXTRA_TITLE` + `EXTRA_TEXT`，用**规则引擎**（正则模板列表，存本地 JSON）提取：金额、方向（收/支）、渠道、对方/商户名。
4. 解析成功 → 写入一条 `status = 待确认` 的账目，发通知「已记录 -￥23.50 星巴克，本月剩余 ￥1,842」。
5. 用户点通知或进 App 确认/修改分类，状态转 `已确认`。解析失败的原始文本入「未识别队列」，可手工补全并「保存为新规则」。

**规则示例（内置）**

| 来源 | 文本样例 | 提取 |
|---|---|---|
| 微信支付 | `微信支付凭证 - 向 星巴克 付款 23.50 元` | 支出 23.50 / 星巴克 |
| 支付宝 | `支付成功 ￥23.50 星巴克咖啡` | 支出 23.50 / 星巴克咖啡 |
| 银行卡 | `您尾号1234卡8月5日支出人民币23.50元` | 支出 23.50 / 银行卡 |

**自动分类**：基于商户名关键词映射表（本地，可编辑），如「星巴克/瑞幸 → 餐饮」「滴滴/高德 → 交通」。命中不到归「未分类」；用户改过一次后记住该商户的分类（学习式映射）。

**手工记账**：始终保留，3 步内完成（金额 → 分类 → 保存）。

**统计**：按日/周/月的收支总额、分类饼图、月度趋势折线、Top 商户。

**已知限制（写明预期，不要指望它 100% 工作）**
- 依赖通知内容，关掉支付 App 的通知就无法记录。
- ColorOS 杀后台会导致监听失效，需引导加白名单（见 9.3）。
- 通知文案改版会让规则失效，因此**必须**保留「未识别队列 + 自定义规则」兜底。
- 不做无障碍服务爬取支付页面——那是隐私性更强、也更脆弱的手段。

#### 3.6.2 预算

**预算设置**
- 月度总预算（必选）：设置每月可支出金额，支持设置账单周期起始日（默认 1 号，可改为发薪日如 15 号）。
- 分类预算（可选）：为「餐饮/交通/购物/娱乐…」各自设上限，分类预算之和不必等于总预算。
- 预算可设为「每月自动沿用上月」或「每月手动确认」。

**每笔支出即时提示（核心体验）**

每记录一笔支出（自动解析成功 或 手工记账保存），立即反馈：

```
┌────────────────────────────────┐
│  ✓ 已记录支出  ￥23.50          │
│  星巴克 · 餐饮                  │
│                                │
│  本月预算剩余  ￥1,842 / ￥3,000  │
│  ▓▓▓▓▓▓▓▓▓░░░░░░░  61%          │
│  餐饮分类剩余  ￥318            │
│  按当前节奏，预计月底超支 ￥120  │  ← 仅在预测超支时显示
└────────────────────────────────┘
```

- **自动记账**触发时：以**系统通知**形式提示（App 在后台），内容含「已记录 -￥X，本月剩余 ￥Y」，点击进确认页。
- **手工记账**保存时：App 内浮层提示，2.5 秒自动消失，进度条带动画增长。
- 进度条颜色随消耗比例过渡：`< 70% 正常色 → 70~90% 警示黄 → > 90% 警告红`，颜色切换有过渡动画。
- 若该笔支出命中某个分类预算，同时显示该分类剩余。

**预算预警**

| 触发条件 | 行为 |
|---|---|
| 单月消耗达 80% | 一次性通知提醒 |
| 单月消耗达 100% | 通知「本月预算已用完」，后续每笔提示改为「已超支 ￥X」 |
| 分类预算超支 | 记账提示中该分类标红 |
| 按日均消耗预测月底会超支 | 记账提示与首页卡片显示预测超支金额 |

预测算法：`预计月末支出 = 已支出 ÷ 已过天数 × 当月总天数`，账单周期跨月时按周期天数计算。

**预算视图**
- 独立「预算」页：总预算环形进度 + 各分类横向进度条 + 剩余天数 + **日均可用余额**（`剩余预算 ÷ 剩余天数`，这个数字比「还剩多少」更有指导意义）。
- 历史月份达成情况回顾（近 12 个月柱状图，超支月份标红）。

**首页卡片**：今日支出合计 + 本月预算进度条 + 待确认笔数徽标。

---

### 3.7 M6 飞书公开网页收藏与阅读

**接入方式**：知识库是**公开分享到互联网的飞书网页**，因此**不需要 OAuth、不需要自建飞书应用、不需要 App Secret**——原方案中最大的安全风险与最重的接入成本直接消除。

**功能**
- **知识源管理页**：手动添加飞书公开链接（`https://xxx.feishu.cn/docx/...` / `/wiki/...` / `/base/...`），可命名、分组、排序、启停、删除。支持从系统分享菜单「分享到 MyAPP」直接添加。
- **阅读**：内嵌 WebView 打开，保留飞书原生排版；顶部自定义工具栏（返回、刷新、外部浏览器打开、加入知识池、复制链接）。
- **正文提取与缓存**：后台在无头 WebView 中加载页面，渲染完成后注入 JS 抽取标题与正文纯文本，存入本地缓存，用于：① 摘要展示 ② 全文搜索 ③ 断网时的降级阅读。
- **离线**：已缓存页面断网可读纯文本版，标注「缓存于 X 时间」；未缓存则提示无网络。
- **搜索**：对已缓存正文做本地全文搜索（FTS），命中后跳转对应页面。
- **收藏/置顶**：常用页面置顶，首页可配快捷入口。

**技术要点与限制（必须知晓）**
- 飞书文档页是 JS 渲染的 SPA，**直接 HTTP 抓 HTML 拿不到正文**。正文提取必须走 WebView 渲染后注入 JS，不能用 Jsoup 之类的静态解析。
- 提取逻辑依赖飞书前端 DOM 结构，飞书改版可能导致失效。因此：**提取失败不影响阅读**（WebView 始终可用），只影响摘要与搜索；提取选择器存本地配置，可在 App 内调整而无需发版。
- 页面必须保持「互联网公开」分享状态。一旦改为需登录，App 检测到后在源列表标记「需要登录，已失效」。
- 正文提取任务放后台低优先级执行（充电 + WiFi 优先），避免影响流畅度与耗电。

**扩展预留**：`KnowledgeSource` 接口保持不变（见 4.7.4），当前实现为公开网页版；将来若需要私有飞书文档，再补一个 OAuth 实现即可，上层无感知。

---

### 3.8 M7 知识点每日推送

- 用户从 M6 的知识源中勾选若干页面/分组组成「知识池」。
- 后台任务每日一次刷新知识池页面的正文缓存（仅 WiFi + 充电时执行完整刷新）。
- 每天挑选一个「知识点」：优先未展示过的；全部展示过后按间隔复习权重排序（1/3/7/15/30 天）。
- 若单个页面内容很长（知识库常见），支持**按章节切分**：以正文中的 H1/H2 标题为界切成多个知识块，每天推一块，避免一次给太多。
- 首页「今日知识点」卡片显示标题 + 摘要（正文前 N 字）+ 来源页面名，点击展开全文或跳转原页面。
- 「已掌握 / 再看看」反馈按钮影响复习权重，卡片带翻转/滑出动画。
- 可选每日定时通知推送。

**降级**：知识池为空或正文提取失败时，从本地笔记（M3）中挑选，保证卡片永不空白。

---

### 3.9 M8 行业新闻（自配置 RSS）

- 订阅源管理页：手动添加 RSS/Atom 地址、命名、分组、启用/禁用、排序、删除。支持 OPML 导入导出。
- 后台定时（默认每 2 小时，可配置；可限仅 WiFi）拉取各源，端上解析 XML，去重（按 guid/link）后入库。
- 列表：全部 / 按分组 / 未读 / 已收藏。显示标题、来源、时间、摘要、封面图（若有）。
- 详情：优先展示 RSS 正文；无正文则 Custom Tabs 打开原链接。
- 操作：已读、收藏（收藏项永久保留）、分享链接、「存为笔记」（一键把标题+链接+摘要存入 M3）。
- 清理策略：非收藏文章保留 30 天，超期自动清理，控制数据库体积。
- 首页显示最新 3 条未读。

---

### 3.10 M9 桌面小组件

用 **Jetpack Glance**（Compose 风格的 AppWidget 框架）实现，与主 App 共用 Repository 层。

**W1 · 今日概览（4×2）**
```
┌──────────────────────────────────────┐
│  8月5日 周三                     ⊕   │
│                                      │
│  今日支出   ￥86.50                   │
│  预算剩余   ￥1,842  ▓▓▓▓▓▓░░░░ 61%   │
│  ──────────────────────────────────  │
│  ○ 交周报                      18:00 │
│  ○ 买牛奶                            │
│  ○ 预约体检                          │
│                        还有 2 项 →   │
└──────────────────────────────────────┘
```
- 上半区：今日支出合计 + 本月预算剩余与进度条（超支时转警告色）。
- 下半区：今日未完成待办前 3 项，**圆圈可直接点击打勾完成**（`actionRunCallback` 直接写库，无需打开 App）。
- 右上 ⊕ 快捷记一笔，点击标题区打开 App 首页。
- 待办为空时显示「今天没有安排 🎉」。

**W2 · 今日支出（2×2）**：今日支出金额 + 预算环形进度，点击进记账页。

**W3 · 今日待办（2×2）**：待办清单，可勾选完成。

**刷新策略**
- 数据变更驱动：新增账目、完成待办、修改预算时主动 `updateAll()`，保证即时。
- 兜底周期刷新：每 30 分钟一次，以及每日 0 点跨天刷新。
- **小组件绝不做网络请求**，只读本地 Room，保证秒开不卡。

**视觉**：跟随 App 主题（浅/深色自动切换）；圆角与留白与主 App 一致。使用小组件预览生成 API（`setWidgetPreview`），让小组件在系统选择器里显示**真实数据预览**而非静态截图。

**验收**：数据变更到界面更新 < 1s；勾选待办后小组件与 App 内状态一致。

---

### 3.11 M0 首页 Dashboard

单页竖向滚动的卡片流，卡片顺序与显隐可在设置中调整：

1. **日期问候** + 纪念日倒数最近一项
2. **今日待办**（未完成项，可直接勾选，空态显示「今天没有安排 🎉」）
3. **经期状态**（距下次 / 第几天 + 快捷「记录开始」按钮）
4. **今日知识点**
5. **今日记账**（今日支出 + 预算进度 + 待确认笔数徽标）
6. **行业动态**（3 条）
7. **一条待解决疑问**

底部导航：`首页 | 记录（笔记/疑问） | 记账 | 资讯 | 我的`
全局悬浮快捷：长按 FAB 展开 → 记笔记 / 记疑问 / 记一笔 / 加待办。

---

## 4. 技术方案

### 4.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Kotlin | — |
| UI | Jetpack Compose + Material 3 | 单 Activity + Navigation Compose |
| 架构 | MVVM + Repository，单向数据流 | ViewModel 暴露 `StateFlow`，UI 无状态 |
| 本地库 | Room（含 FTS 全文搜索） | 唯一真相来源 |
| 异步 | Coroutines + Flow | 所有 IO 走注入的 Dispatcher |
| DI | Hilt | 含 `@IntoSet` 多绑定，支撑首页插件机制 |
| 网络 | OkHttp + Retrofit + kotlinx.serialization | RSS 拉取 |
| RSS 解析 | XmlPullParser（系统自带） | 避免重依赖 |
| 网页阅读 | WebView（内嵌 + 无头提取） | 飞书公开页 |
| 后台任务 | WorkManager（可延迟任务）+ AlarmManager（精确提醒） | 分工见 4.3 |
| 小组件 | Jetpack Glance | 与 App 共用 Repository |
| 图片 | Coil | — |
| 偏好存储 | DataStore | — |
| 动效底座 | Material 3 Expressive（MotionScheme + 形状变形） | 只取动效引擎，不取默认配色，见 9.2.1 |
| 触感 | 系统振动 API（Android 16 增强包络） | 关键动效配同步触感 |
| 构建 | Gradle 版本目录 + build-logic 约定插件 | 见 4.8 |
| 版本 | **minSdk 35 / targetSdk 36 / compileSdk 36** | 见 9.2 |

### 4.2 数据模型（Room 表）

```
period_record       (id, uuid, start_date, end_date?, note, created_at, updated_at)
anniversary         (id, uuid, title, date, is_lunar, repeat_type, remind_days_before, note, ...)
todo                (id, uuid, title, note, due_at?, priority, tags, repeat_rule?,
                     done, done_at?, created_at, updated_at, deleted_at?)
note                (id, uuid, content, tags, images_json, pinned, created_at, updated_at)
note_fts            (FTS 虚表，映射 note.content)
question            (id, uuid, content, context?, tags, status, answer?, created_at, resolved_at?)

transaction_record  (id, uuid, amount, direction, category, merchant, channel, occurred_at,
                     status /*pending|confirmed*/, raw_text?, source /*auto|manual*/, ...)
budget              (id, cycle_start_day, total_amount, effective_from, effective_to?, auto_rollover)
budget_category     (budget_id, category, amount)
merchant_category_map (merchant_key, category)        -- 学习式分类
parse_rule          (id, package_name, regex, field_map_json, enabled, priority)

knowledge_source    (id, url, title, group_name, enabled, sort_order, pinned,
                     in_pool, last_fetch_at, fetch_status)
knowledge_content   (source_id, section_index, section_title, content_text, fetched_at)
knowledge_content_fts (FTS 虚表)
knowledge_review    (source_id, section_index, last_shown_at, shown_count, mastery_level)

rss_feed            (id, url, title, group_name, enabled, sort_order, last_fetch_at)
rss_article         (id, feed_id, guid, title, summary, content?, link, cover?,
                     published_at, is_read, is_starred)
```

**全局字段约定**（为将来同步预留，现在就要有，后补代价极大）：
- `uuid`：全局唯一标识，跨设备同步的前提
- `created_at` / `updated_at`：审计与冲突解决基础
- `deleted_at`：需要软删除的表必须有——同步需要 tombstone，硬删除会导致删除操作无法传播

**索引**：所有按时间/日期查询的列建索引；`rss_article.guid` 唯一索引用于去重。

**迁移纪律**：本项目数据**无云端备份，丢一次就永久没了**。因此严禁 `fallbackToDestructiveMigration()`；每次改表必须写 Migration 并提交 `schemas/` 下的 JSON；迁移要有测试。

**金额存储**：一律用「分」存 `Long`，绝不用 `Float`/`Double`——浮点累加误差在记账场景是不可接受的。

### 4.3 后台任务分工

| 任务 | 机制 | 频率 |
|---|---|---|
| 待办/纪念日/经期提醒 | **AlarmManager 精确闹钟** | 逐条注册，开机广播重建 |
| 支付通知解析 | NotificationListenerService | 事件驱动 |
| RSS 拉取 | WorkManager 周期 | 默认 2h，需网络 |
| 知识池正文刷新 | WorkManager 周期 | 每日一次，WiFi + 充电 |
| 每日知识点选取 + 通知 | WorkManager 定时 | 每日 08:00 |
| RSS 旧数据清理 | WorkManager | 每周一次 |
| 小组件刷新 | 数据变更主动触发 + 周期兜底 | 见 3.10 |

> **关键分工原则**：定时提醒**一律用 `setExactAndAllowWhileIdle`**，不用 WorkManager。ColorOS 会冻结后台，WorkManager 的周期任务在这台机器上不可靠（见 9.3）。WorkManager 只用于「晚一点执行也无所谓」的任务。

### 4.4 权限清单

| 权限 | 用途 | 时机 |
|---|---|---|
| `INTERNET` / `ACCESS_NETWORK_STATE` | RSS + 网页 | 安装即有 |
| `POST_NOTIFICATIONS` | 所有提醒 | 首启引导 |
| `USE_EXACT_ALARM` | 精确提醒 | **安装即授予，无需引导**（见 9.2） |
| `SCHEDULE_EXACT_ALARM` | 精确提醒兜底 | 保留声明 |
| `RECEIVE_BOOT_COMPLETED` | 重启后重建闹钟 | 安装即有 |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 引导加电池白名单 | 保活自检时 |
| `VIBRATE` | 动效触感反馈 | 安装即有 |
| 通知使用权（特殊权限） | 自动记账 | 用户开启该功能时 |
| ~~`READ_MEDIA_IMAGES`~~ | 笔记插图 | **不需要**——用 Photo Picker，免权限 |

### 4.5 性能要求

- 冷启动到首页可交互 **< 800ms**（Baseline Profile + 延迟初始化非首屏依赖）。
- 首页每张卡片独立 Flow，任一模块加载慢不阻塞整页；卡片先出**骨架屏**（不用转圈，转圈会让首页看起来在卡）。
- 单张卡片的数据源异常只让那张卡片显示错误态，**不能拖垮整页**。
- 列表统一 `LazyColumn` + 稳定 key + 分页（RSS 与账目用 Paging），144Hz 下稳定不掉帧。
- 严禁主线程 IO（debug 下开 StrictMode 检查）。
- Release 包开 R8 + 资源压缩，目标 APK < 15MB。

### 4.6 数据安全与备份

- 全部数据在 App 私有目录，不申请外部存储读写。
- **关闭系统云备份与设备迁移**（数据均为隐私内容，且迁移路径是应用内导出）。
- 备份：设置页「导出全部数据」→ 生成加密 zip（Room DB + 图片）到用户选定位置（SAF）；「导入」覆盖恢复。**这是无服务器方案下换机的唯一路径**，必须在首启引导里强调定期导出。
- 可选：应用锁（生物识别 / PIN）保护进入。

### 4.7 可扩展性设计（重点）

目标：**新增一个功能模块 = 新建一个 module + 注册一次，不改动任何已有模块的代码。**

#### 4.7.1 模块化工程结构

```
:app                        组装层。唯一知道「所有 feature 都存在」的地方
:core:common                时间/协程扩展、Result 封装、跨模块协作契约
:core:designsystem          色彩、字体、形状、MotionTokens、通用组件
:core:database              Room 实例、实体、DAO、迁移
:core:datastore             偏好设置、功能开关
:core:network               OkHttp/Retrofit 基建
:core:ui                    导航契约 Route、首页卡片插槽 HomeCard
:feature:home               首页：只做卡片编排，不认识具体业务
:feature:todo               待办（同时是新增模块的参考实现）
:feature:period / anniversary / note / question / ledger / knowledge / feed / widget / settings
```

**依赖规则（不可违反）**
1. `:feature:*` **之间永远不互相依赖**
2. `:feature:*` 只能依赖 `:core:*`
3. 需要跨 feature 协作时 → 在 `:core:common/contract` 定义接口，提供方实现并 Hilt 绑定，调用方只注入接口

> 例：资讯页的「存为笔记」不 import `:feature:note`，而是注入 `NoteWriter`。这样删掉 `:feature:note` 时，只需换一个空实现，资讯页一行都不用改。

#### 4.7.2 首页卡片 = 插件机制

首页不硬编码任何业务卡片，只消费一个卡片注册表：

```kotlin
interface HomeCard {
    val id: String            // 唯一标识，用于持久化用户排序/显隐；一旦发布不要改
    val defaultOrder: Int
    fun isEnabled(): Flow<Boolean>
    @Composable fun Content(onNavigate: (Route) -> Unit)
}
```

- 每个 feature 通过 Hilt `@Binds @IntoSet` 把自己的 `HomeCard` 注入进来。
- `HomeViewModel` 注入的是 `Set<HomeCard>`，按配置排序渲染。
- **新增一个首页卡片 = 实现接口 + 绑定一次，首页代码一行不改，也不需要 import 你的模块。**
- 契约要求：`Content` 必须自己处理 Loading / Error / Empty 三态，不能把异常抛给首页。

#### 4.7.3 导航契约

- 路由用密封接口 `Route` 定义在 `:core:ui`，配合类型安全导航，避免字符串拼路由。
- feature 只声明自己的 `NavGraphBuilder` 扩展函数（如 `todoGraph()`）。
- `:app` 在导航图里逐个调用注册。**feature A 想跳转到 feature B 的页面时只依赖 Route 契约，不依赖 feature B 本身**。

#### 4.7.4 数据源可插拔

对「未来可能换实现」的地方全部先定义接口：

| 接口 | V1 实现 | 未来可替换为 |
|---|---|---|
| `KnowledgeSource` | 飞书公开网页（WebView 提取） | 飞书 OAuth 私有文档 / Notion / 本地 Markdown |
| `NoteWriter` | 本地 Room | 飞书云文档 / WebDAV |
| `LedgerWriter` | 本地 Room | — |
| `ReminderScheduler` | AlarmManager | — |
| `SyncProvider` | 无（仅本地导出） | WebDAV / 坚果云 / 私有服务器 |

`SyncProvider` 尤其重要：V1 虽然不做服务器，但**接口先留好**，将来接同步不需要重构数据层。

#### 4.7.5 记账规则引擎可扩展

- 解析规则不写死在代码里，存本地 JSON（`parse_rule` 表 + assets 内置默认规则）。
- 规则结构：`{ package, matcher(regex), fields: {amount, merchant, direction}, priority }`。
- 支持在 App 内新增/编辑规则，也支持导入规则包 → **新增一个支付渠道无需发版**。
- 解析器按 priority 依次尝试，命中即止；未命中进未识别队列，可从原文一键生成新规则。

#### 4.7.6 配置与开关

- 所有可配置项走 DataStore，key 用 `<feature>.<name>` 命名空间，避免不同模块撞车。
- 功能开关本地化：每个 feature 可被整体关闭，关闭后其卡片、导航入口、后台任务同时失效——便于试验性功能安全落地。

#### 4.7.7 扩展性验收标准

- 新增一个「喝水打卡」类简单模块，**改动的已有文件 ≤ 3 个**（`settings.gradle.kts`、`app/build.gradle.kts`、`AppNavHost.kt`）；**首页完全不用动**。
- 删除任一 feature module 后，项目仍能编译通过。

### 4.8 骨架落地情况（已完成）

项目骨架已搭建，包含 build-logic 约定插件、6 个 core 模块、`:feature:home` 与 `:feature:todo`（插件机制的完整参考实现）。详见 `docs/开发指南.md`。

**约定插件**：一个 feature 模块的完整构建脚本只有 6 行——

```kotlin
plugins { alias(libs.plugins.myapp.android.feature) }
android { namespace = "com.myapp.feature.todo" }
```

library + Compose + Hilt + 全部 core 依赖都在 `myapp.android.feature` 插件里配好，不用复制粘贴。

**与本文档的一处刻意偏离**：4.7.1 原写「每个 feature 自带独立的 Entity/DAO」，实际实现把实体与 DAO 集中放在 `:core:database`（按 feature 分包）。原因：Room 的 `@Database` 需要编译期拿到完整实体列表，无法运行时注册；坚持原设计会让 `:core:database` 反向依赖所有 feature，依赖方向就乱了。这也是 Google 官方 Now in Android 的做法。扩展性未受损：新增 feature 时在 `model/` 与 `dao/` 下加包即可，不影响任何已有模块。

---

## 5. UI 设计规范（Claude 风格）

设计目标：**简洁而优雅**——克制的色彩、充足的留白、清晰的层级、一个温暖的强调色。不用重色块、不用高饱和渐变、不堆装饰。

### 5.1 色彩

| 角色 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| 背景 Base | `#FAF9F5`（暖米白） | `#1F1E1D` | 页面底色，**不用纯白/纯黑** |
| 卡片 Surface | `#FFFFFF` | `#262624` | 卡片、弹层 |
| 次级容器 | `#F3F1EA` | `#302F2C` | 输入框底、选中态 |
| 边框 Border | `#E8E5DC` | `#3A3937` | 1px 细描边，替代重阴影 |
| 主文字 | `#191917` | `#F5F4EF` | 标题正文 |
| 次文字 | `#6B6960` | `#A3A099` | 说明、时间戳 |
| 三级文字 | `#9A978D` | `#75726B` | 弱化信息、未选中图标 |
| 强调 Accent | `#D97757`（陶土橙） | `#E08A6C` | 按钮、进度、选中态、图表主色 |
| 成功 | `#5C8A5C` | `#7FA97F` | 完成、预算健康 |
| 警示 | `#C9A227` | `#D9B84A` | 预算 70~90% |
| 危险 | `#C0553C` | `#D4674C` | 超支、逾期、删除 |

**三条不可动摇的规则**
1. 不用纯白 `#FFFFFF` 作背景，不用纯黑 `#000000` 作深色底——一律暖调；
2. 层级靠「底色差 + 1px 描边」表达，**不靠阴影**（阴影在暖米白上会发灰、显脏）；
3. **一屏最多一个 Accent 焦点**。多个 CTA 时只有主操作用 Accent，其余用描边或纯文字按钮。

**分类色板**：记账饼图、标签使用低饱和莫兰迪色系（陶土/橄榄/雾蓝/芥末/藕荷/松绿/赭石/灰褐），与 Accent 同色温。**绝不用彩虹色**——那是廉价感的主要来源。按稳定 key 取色，保证同一分类每次颜色一致。

**深色模式是独立设计**而非简单反色，暖色调保持一致。系统动态取色（Material You）作为**可选项，默认关闭**——默认保持品牌感，这是刻意的设计决定。

### 5.2 字体与排版

- **标题**：衬线体，营造「文档感/高级感」——`Noto Serif SC`（中文必须有衬线覆盖，否则中英混排会掉字重）。
- **正文/UI**：无衬线，`MiSans`（免费商用）。
- **数字**：金额、倒数天数使用等宽数字（`fontFeatureSettings = "tnum"`），否则数字变化时宽度会跳。
- 字号阶梯：`32 / 24 / 20 / 16 / 14 / 12`，行高 1.5，中文字间距 `0.01em`。
- 段落最大宽度限制，长文阅读页两侧留白充足。

### 5.3 形状与空间

- 圆角：卡片 `16dp`，按钮/输入框 `12dp`，小标签 `8dp`，底部弹层 `28dp`。
- **阴影极轻或不用**，层级靠底色差 + 1px 描边（这是 Claude 风格的关键）。
- 间距基准 `4dp` 栅格：卡片内边距 `16~20dp`，卡片间距 `12dp`，页面左右边距 `20dp`。
- 卡片是主要容器单元，首页即卡片流。

### 5.4 组件规范

- **按钮**：主按钮实心 Accent + 白字；次按钮描边；三级纯文字。高度 `48dp`，按下轻微缩放（0.97）。
- **输入框**：无填充底色，仅 1px 描边，聚焦时描边转 Accent 并有 150ms 过渡。
- **卡片按压**：用轻微缩放（0.985）替代默认涟漪——涟漪显廉价。
- **空态**：一句简短友好的文案 + 一个主操作按钮，**绝不留白屏**。文案要具体、带一点温度，不要用「暂无数据」这种系统腔。
- **骨架屏**：所有异步卡片先显示 shimmer 骨架，不用转圈 Loading。
- **Toast/浮层**：从底部滑入的圆角浮层，带图标，2.5s 自动消失。

---

## 6. 动效设计与组件库

原则：**动画服务于反馈与层级，不做无意义炫技**。每个动画必须回答「它解释了什么」——元素从哪来、到哪去、状态怎么变。同时严守性能底线（见 6.4）。

### 6.1 动画组件库选型

| 库 | 用途 | 备注 |
|---|---|---|
| **Material 3 Expressive**（官方，首选底座） | 全局动效令牌 `MotionScheme`、形状变形、表现力组件（加载指示器、FAB 菜单） | **只取动效与形变，配色/圆角/字体沿用第 5 章自定义规范**，详见 9.2.1 |
| **Compose Animation**（官方） | 90% 的动画：`animate*AsState`、`AnimatedContent`、`AnimatedVisibility`、`updateTransition` | 基础，全部基于 spring 物理曲线 |
| **Shared Element Transition**（官方） | 列表 → 详情的元素连续过渡（卡片放大成页面） | **高级感的最大来源**，优先用官方而非第三方 |
| **Lottie Compose** | 复杂矢量插画：空态插画、完成庆祝、引导页 | 控制在 5 个以内避免包体膨胀 |
| **Haze** | 实时毛玻璃：底部导航栏、悬浮工具栏、弹层背景 | — |
| **AGSL RuntimeShader** | 高级质感：知识点卡片流体渐变、预算环形光晕、下拉刷新波纹 | minSdk 35，可直接用 |
| **Konfetti** | 达成类庆祝：完成全部今日待办、纪念日当天、月度未超支 | 克制使用，每种场景每天最多一次 |
| **Canvas 自绘 / 图表库** | 记账图表入场动画（柱状生长、折线描画、饼图展开） | 统一 600ms ease-out |

### 6.2 关键动效清单

| 场景 | 动效 |
|---|---|
| App 启动 | Splash Screen API → Logo 缩放淡出直接过渡到首页。启动图背景色必须与 Base 色一致，否则会闪一帧白屏 |
| 首页卡片入场 | 自上而下依次淡入 + 上移，间隔 40ms 形成瀑布感（**仅首次进入**，重组不重播） |
| 卡片 → 详情页 | Shared Element：卡片圆角与位置连续变形为详情页头部 |
| 底部导航切换 | 图标微弹（spring）+ 页面 fade-through（不做左右滑，竖向卡片流里左右滑显廉价） |
| FAB 长按展开 | 子按钮沿弧线依次弹出（stagger 30ms），背景 Haze 模糊 + 变暗 |
| 完成待办 | 复选框描边收拢成勾 + 文字划线 + 条目滑出列表 |
| 全部待办完成 | Konfetti 短促庆祝 + 空态文案淡入 |
| **记账成功提示** | 浮层从底部弹入（spring 带轻微过冲）→ 金额从 0 滚动到目标值 → 预算进度条 800ms 平滑推进 → 跨越 70%/90% 阈值时颜色渐变过渡 |
| 预算超支 | 进度条抖动一次（±4dp，2 个来回）+ 转危险色 |
| 每日知识点 | 卡片轻微 3D 翻转展示新内容；「已掌握」后向右滑出并缩小 |
| 经期日历 | 月份切换水平滑动，日期色块以中心为原点依次缩放淡入 |
| 下拉刷新 | Accent 色圆点形变 + 顶部液态波纹（AGSL） |
| 列表滚动 | 顶部栏随滚动收缩（标题从大号衬线缩为小号）+ 底部导航滑动隐藏/显示 |
| 长文阅读 | 顶部阅读进度条随滚动填充 |
| 深浅色切换 | 全局 300ms 颜色过渡，非瞬变 |

### 6.3 动效参数规范

统一定义在 `:core:designsystem` 的 `MotionTokens`，**禁止各处硬编码 `tween(300)`**——那会让全局手感不一致，且很难统一降级。

```kotlin
object MotionTokens {
    const val DurationMicro = 120       // 微反馈：按压、勾选
    const val DurationStandard = 280    // 常规过渡
    const val DurationEmphasized = 450  // 大位移、共享元素
    const val DurationCounter = 700     // 数值滚动
    const val StaggerDelayMs = 40       // 列表错峰

    fun <T> enterSpring() = spring<T>(dampingRatio = 0.8f, stiffness = StiffnessMediumLow)
    fun <T> exitTween() = tween<T>(180, easing = FastOutLinearInEasing)  // 退场要快
    // ...
}
```

时长基准：微反馈 `100~150ms`，常规过渡 `250~300ms`，大位移 `400~500ms`，数值滚动 `600~800ms`。**任何动画不超过 1s**。

底座接 Material 3 Expressive 的 `MotionScheme`，`MotionTokens` 只保留项目特有的补充令牌。

### 6.4 性能与可访问性底线

- 144Hz 屏**帧预算仅 6.9ms**（60Hz 是 16.6ms）；用 Macrobenchmark（**按 144Hz 配置基线**）监控，首页滚动丢帧率 < 1%。
- 优先用 `graphicsLayer` 属性动画（translation/scale/alpha），**严禁在动画中触发 layout/measure**。
- 因 minSdk 35，Haze 模糊与 AGSL 着色器**可直接全量启用，无需写降级分支**。
- 关键动效配同步触感反馈，**触感与视觉必须同帧触发**，错位会显得廉价。
- 尊重系统「移除动画」无障碍设置 → 全部动画降为瞬时。
- 设置页提供「动效强度：完整 / 精简 / 关闭」三档开关。
- Lottie 资源总计控制在 500KB 内。

---

## 7. 交付计划

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **P0（第 1 期）** | 项目骨架与模块划分、Room、**设计系统定稿**、首页 Dashboard、M1 经期&纪念日、M2 待办提醒、M3 笔记、M4 疑问 | 可日常使用的本地版 |
| **P1（第 2 期）** | M5 自动记账 + **预算与即时提示**、M9 桌面小组件、M6 飞书公开页收藏与阅读、设置页与保活自检 | 记账闭环 + 桌面入口 |
| **P2（第 3 期）** | M7 每日知识点与复习、M8 RSS 资讯、数据导出导入、应用锁 | 功能完整版 |
| **P3（打磨）** | 高级动效（共享元素、Haze、AGSL、Lottie）、性能优化（Baseline Profile + Macrobenchmark）、深色模式精修、小组件多尺寸 | 体验版 |

> **设计系统必须在 P0 就定稿并落成代码**（`:core:designsystem`）。若等到 P3 再统一 UI 风格，前面所有页面都要返工。（已完成）

---

## 8. 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| **realme UI（ColorOS）杀后台**导致提醒/记账/小组件失效 | **最高** | 首启「保活自检」向导逐项引导（9.3）；提醒用精确闹钟穿透 Doze；记账保留手工补录与未识别队列；小组件走主动刷新 |
| 无服务器 → 换机丢数据 | 高 | 强提示定期导出备份；首启引导中说明 |
| 支付通知文案改版 → 规则失效 | 中 | 未识别队列 + 用户自定义规则；规则存 JSON 便于迭代 |
| 飞书前端改版 → 正文提取失效 | 中 | WebView 阅读始终可用，只影响摘要/搜索；提取选择器可在 App 内配置 |
| 分享链接被改为「需登录」 | 中 | 检测并在源列表标记失效，提示重新公开或替换链接 |
| 动效堆叠导致掉帧、发热 | 中 | MotionTokens 统一管控 + 动效强度三档 + Macrobenchmark 卡口 |
| WebView 无头提取耗电 | 中 | 仅 WiFi + 充电时批量刷新，限制并发与超时 |
| 数据库迁移出错 | 中 | 禁用破坏性迁移，schema 入版本控制，迁移必须有测试 |

---

## 9. 目标设备适配：realme GT Neo5 SE

本 App 单人自用，**唯一目标设备已确定**。这带来两个直接影响：可以砍掉大量兼容分支；但必须重点处理 realme UI（ColorOS 内核）的后台限制。

### 9.1 设备规格与技术基线

| 项目 | 设备情况 | 对方案的影响 |
|---|---|---|
| SoC | 骁龙 7+ Gen 2 | 性能充裕，**Haze 毛玻璃、AGSL 着色器、共享元素转场可全量开启**，不必按低端机降级 |
| 屏幕 | 6.74" 1.5K AMOLED，**144Hz** | 动画性能目标 **144fps**，帧预算仅 **6.9ms** |
| 内存 | 8GB 起 | 缓存策略可放宽 |
| 系统 | **realme UI 7.0 / Android 16（API 36）** | minSdk 35 / targetSdk 36 |
| 屏幕形态 | 直屏、居中挖孔 | 无曲面误触问题，需处理挖孔区的沉浸式布局 |

### 9.2 版本策略：minSdk 35 / targetSdk 36

设备已是 Android 16，可以把版本基线定得很高。**取 minSdk 35 而非直接 36**：API 33~35 已囊括本项目 99% 的能力收益，36 独有的只有增强触感等少数增量 API，用一行版本判断包住即可；留一点余量，将来换机或在备用机上跑不至于装不上。

**可以直接删掉的兼容代码**

| 能力 | 需要 API | 收益 |
|---|---|---|
| AGSL RuntimeShader | 33 | 液态下拉刷新、光晕**无需降级分支** |
| RenderEffect 高斯模糊 | 31 | 毛玻璃直接用，无 fallback |
| `POST_NOTIFICATIONS` 运行时权限 | 33 | 只写一套权限逻辑 |
| Photo Picker | 33 | **完全不申请存储权限** |
| `USE_EXACT_ALARM` | 33+ | 精确闹钟免用户授权 |
| 可预测返回手势 | 35 默认开启 | 直接实现 |
| 小组件预览生成 API | 35 | 小组件在选择器里显示真实预览 |
| Edge-to-edge | 35 起强制 | 只写一套沉浸式布局 |

**targetSdk 36 的强制项（必须处理，不是可选）**
- **Edge-to-edge 强制生效、无法 opt-out**。所有页面必须正确处理 `WindowInsets`，否则内容会被状态栏/导航栏遮挡。设计上本就要做全屏沉浸，方向一致。
- **前台服务必须声明类型**。`NotificationListenerService` 不属于前台服务，不受影响；将来加前台服务需补声明。
- 若引入 native 依赖，需支持 **16KB 内存页**。本项目纯 Kotlin/Java 依赖，暂不涉及。

**精确闹钟权限的简化**
核心功能为提醒/闹钟的应用可声明 `USE_EXACT_ALARM`，**安装即授予，无需引导用户手动开启**——正好覆盖待办提醒、纪念日、经期预测。相比 `SCHEDULE_EXACT_ALARM`（需跳系统设置手动开）体验好很多。
> 该权限在 Google Play 上架有严格政策限制，但本项目**不上架、仅自用**，可放心使用。仍保留 `SCHEDULE_EXACT_ALARM` 作为兜底声明。

### 9.2.1 Material 3 Expressive（直接服务于「炫酷高级动画」）

Material 3 Expressive 是本项目动效方案的最大加分项，且**它是 Compose 库能力、不依赖系统版本**，但在 Android 16 上与系统观感最一致：

- **Motion Scheme**：官方提供一整套基于弹簧物理的动效令牌（`MotionScheme.expressive()`），可直接接入 `MaterialTheme`，全局统一动效手感。作为 `MotionTokens` 的底座。
- **形状变形（Shape Morphing）**：内置多边形形状库与形变动画，用于加载指示器、按钮按压形变、进度环——比自己用 Path 手撸稳定得多。适合预算环形进度、下拉刷新。
- **表现力组件**：新的加载指示器、FAB 菜单（正好用于首页 FAB 长按展开）、工具栏动效，可省掉自研成本。

**取舍说明**：M3 Expressive 默认观感偏活泼、色彩偏鲜艳，与 Claude 的克制风格**有冲突**。方案是——**只取其「动效引擎与形变能力」，不取其默认配色与圆润外观**：色彩、圆角、字体全部沿用第 5 章自定义规范，动效层接 Expressive 的 MotionScheme。这样既拿到高级动效，又保住简洁优雅的基调。

**增强触感**：Android 16 提供更精细的振动包络 API，让关键动效配上恰当的触感反馈（完成待办的轻点、预算超支的双击警示、FAB 展开的细微反馈）。触感与动画同步是「高级感」最容易被忽略的一环。

**落地前提**：需要 `material3` **1.4.0 及以上**。首次同步项目后确认版本，到位后在 `Theme.kt` 中放开 `motionScheme = MotionScheme.expressive()`。

### 9.3 realme UI 后台限制（最高风险项）

ColorOS 系族的后台管理是国产 ROM 里最激进的之一，**会直接影响自动记账、定时提醒、小组件刷新**这三个核心能力。且**不会因为系统升级到 Android 16 而放松**——这是 OEM 层策略，别指望新系统能救。

必须在首启引导里做一个「保活设置向导」，逐项跳转并说明：

| 设置项 | 路径（realme UI） | 不设置的后果 |
|---|---|---|
| 允许自启动 | 设置 → 应用管理 → MyAPP → 耗电管理 → 允许自启动 | 重启后闹钟不重建、通知监听不恢复 |
| 允许后台活动 | 同上 | WorkManager 周期任务被杀，RSS/知识点不更新 |
| 电池优化白名单 | 设置 → 电池 → 省电与优化 → MyAPP → 不限制 | 后台被冻结，通知监听失效 |
| 通知使用权 | 设置 → 通知与状态栏 → 通知使用权 → MyAPP | 自动记账完全不工作 |
| 锁定后台任务 | 最近任务界面下拉卡片 → 锁定 | 手动清理时被误杀 |
| 关闭智能省电对本应用生效 | 设置 → 电池 | 深度睡眠时闹钟延迟 |

**引导做法**：设置页提供「保活自检」入口，逐项检测当前状态（能检测的用 API，如 `isIgnoringBatteryOptimizations`、`NotificationManagerCompat.getEnabledListenerPackages`），未通过的给一键跳转按钮 + 图文说明。首次安装强制走一遍。

**兜底设计（不能只靠用户设置对）**
- 提醒类**一律用 `AlarmManager.setExactAndAllowWhileIdle`**，能穿透 Doze。
- 监听 `ACTION_BOOT_COMPLETED` + `ACTION_MY_PACKAGE_REPLACED`，重建全部闹钟与通知监听绑定。
- `NotificationListenerService` 被杀后系统通常会重启它；额外做每日自检，发现绑定丢失则通知用户。
- 自动记账**永远保留手工补录**与未识别队列，不假设自动一定成功。
- 小组件以「数据变更主动刷新」为主、周期刷新为辅，不完全依赖系统调度。

### 9.4 144Hz 高刷适配

- **帧预算 6.9ms**，比 60Hz 严苛一倍多。所有动画必须走 `graphicsLayer`，严禁在动画中触发 layout/measure。
- 动画一律用 **spring 物理曲线**而非固定帧数插值，天然适配任意刷新率。
- realme UI 默认刷新率可能是「自动选择」；如遇动画期间掉到 60Hz，可在关键页面显式请求高刷（Android 15+ 已有更完善的帧率协商机制，通常无需手动干预）。
- Macrobenchmark 的 jank 判定基线要按 **144Hz 配置**，否则按 60Hz 测出来的「无卡顿」是假的。
- 高刷 + 1.5K 下过度绘制代价明显：卡片背景、模糊层、阴影避免多层叠加，控制在 2x 以内。

### 9.5 AMOLED 与视觉

- 深色模式用**接近纯黑的暖深色**（`#1F1E1D`）而非中灰，AMOLED 上更省电、对比更好；但不用纯黑 `#000000`，避免与暖调冲突、也避免滚动时的黑边拖影。
- 挖孔屏：全屏沉浸，内容用 `WindowInsets` 避让状态栏，顶部渐隐处理。
- 小组件在深色下需保证对比度。

### 9.6 开发与调试注意

- realme UI 需在开发者选项中开启 **USB 调试** 和 **「禁止权限监控」**（否则 adb 安装的 debug 包会被反复拦截权限）。
- ColorOS 对 debug 包有额外安装拦截。**建议自己签一个固定 keystore 出 release 包日常使用**：R8 优化后才是真实性能，且系统不会区别对待。
- 环境准备与新增模块流程见 `docs/开发指南.md`。

---

## 10. 待确认项

1. **记账分类体系**用默认一套（餐饮/交通/购物/娱乐/居住/医疗/人情/其他），还是自定义？影响预算分类粒度。**这个最影响实现，开工 P1 前需定。**
2. 预算周期起始日默认 1 号，是否需要改成发薪日？
3. 中文字体：默认选 MiSans（免费商用）+ Noto Serif SC，有偏好可替换。
4. 小组件是否需要「纪念日倒数」尺寸？当前只做支出与待办两类。
5. 首次同步项目后需核对 `material3` 版本是否已含 `MotionScheme` 等 Expressive API（库能力，与系统版本无关）。
