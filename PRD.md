# MyAPP 产品需求文档（PRD）

- 版本：v1.6
- 日期：2026-08-13
- 平台：Android 原生（单人自用，不上架）
- 目标设备：**realme GT Neo5 SE**（骁龙 7+ Gen 2 / 144Hz 1.5K AMOLED / **realme UI 7.0 · Android 16**），minSdk 35 · targetSdk 36
- 核心约束：**无自建服务器**，所有数据本地存储，仅直连公开网页与 RSS；云备份（3.13）是唯一例外，使用第三方 BaaS（腾讯云开发）托管本人数据的加密备份快照，不算「自建服务器」，且默认关闭、不影响本地优先架构
- 设计基调：Claude 风格——简洁、优雅、暖色调，配以克制而高级的动效

**版本变更记录**

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿：8 个功能模块、本地优先架构、可扩展性设计 |
| v1.1 | 飞书接入由 OAuth 改为**公开网页**；新增**预算**功能与每笔支出即时提示；新增**桌面小组件**；新增 UI 设计规范与动效方案两章 |
| v1.2 | 确定目标设备为 realme GT Neo5 SE，新增设备适配章节 |
| v1.3 | 设备系统确认为 **Android 16**，版本基线提至 minSdk 35 / targetSdk 36；动效底座接入 **Material 3 Expressive**；补充骨架落地情况 |
| v1.4 | 第 10 章四项待确认全部定稿：记账分类**默认集 + 可自定义**；预算周期改为**发薪日 10 号起算**且可调，全局改用「本期」口径；中文字体定为 **Noto Serif SC + MiSans** 并要求内嵌子集化；小组件补齐为四个、新增**纪念日倒数**并明确不做经期小组件。M2 待办四视图与编辑页落地 |
| v1.5 | 第 10 章清空：确认 `material3 1.4.0-alpha13` 已含 `MotionScheme`，Expressive 动效底座**已启用**；纪念日小组件默认对象定为**置顶 → 最近 → 最早创建 → 引导卡**四级链。**M1 纪念日 & 经期记录落地**：自研离线农历换算（1900~2100，全范围往返测试）、纪念日四类倒数、经期预测与日历；数据库升至 v2 并补 `MIGRATION_1_2`（已在真机上从 v1 实测升级成功）。修复一个会让**所有带 label 的输入框崩溃**的排版缺陷（`letterSpacing` 混用 em/sp），并追加 5.2 的单位约束 |
| v1.6 | **新增 3.13 云数据备份**：核心约束「无自建服务器」松绑为「无自建服务器，可用第三方 BaaS 做本人数据的加密备份」——引入腾讯云开发（CloudBase，PostgreSQL REST API）作为可选云备份通道，实现 4.7.4 早已预留但一直空置的 `SyncProvider` 接口。定位是**本地导出/导入的补充，不是替代**：断网/未绑定云备份时，App 全部功能与既有的本机导出/导入不受影响。改写 4.6、4.7.4、第 8 章风险登记 |

---

## 1. 产品定位

一个「个人生活 + 知识 + 信息」聚合的私人 App。把日常记录（纪念日/经期/待办/记账）、个人知识库（飞书公开页）、行业信息（RSS）收在一个入口里，随手记、随手查。

**设计原则**

| 原则 | 含义 |
|---|---|
| 本地优先 | 所有个人数据落在本机 Room 数据库，断网可用；网络只用于网页抓取和 RSS 拉取 |
| 无服务器 | 不做任何后端。知识库走公开网页直连；新闻走端上 RSS 抓取解析 |
| 流畅 | 冷启动 < 800ms，144Hz 下滚动不掉帧，所有 IO 在协程后台，UI 只消费 Flow |
| 单人自用 | 不做多用户、不做权限体系、不做账号系统；安全边界 = 手机本身 |

**非目标（明确不做）**

- 不做多设备实时同步（提供手动本地导出/导入 + 可选云端备份两条换机路径，均非实时同步）
- 不做社交、分享、协作
- 不自建服务器、不做账号体系/多用户；云备份（3.13）用已单独开通的第三方 BaaS 账号，只作为「本人数据的异地存档」，不是产品账号系统

---

## 2. 用户与场景

唯一用户：你本人。

典型场景：
1. 早上打开 App → 首页看到「今日待办 + 日程提醒 + 对象经期预测 + 今天的知识点卡片 + 3 条行业新闻」。
2. 想到一件事 → 2 秒内记一条「疑问」或「笔记」，不打断当前事情。
3. 收到微信/支付宝支付通知 → App 后台自动记一笔账，并立刻告诉你本月预算还剩多少。
4. 不打开 App，扫一眼桌面小组件就知道今天花了多少、还有什么没做。
5. 周末回顾 → 看本周花销、看未解决的疑问列表、翻知识库。

---

## 3. 功能范围

### 3.1 模块总览

| # | 模块 | 优先级 | 依赖 |
|---|---|---|---|
| M1 | 纪念日 & 经期记录 | P0 | 本地 |
| M2 | 待办与日程提醒 | P0 | 本地 + 通知 |
| M3 | 快速笔记 | P0 | 本地 |
| M4 | 疑问收集箱 | P0 | 本地 |
| M5 | 记账（通知栏自动解析 + 预算） | P1 | 通知监听权限 |
| M6 | 飞书公开网页收藏与阅读 | P1 | 网络（无需授权） |
| M7 | 知识点每日推送 | P1 | 依赖 M6 |
| M8 | 行业新闻（自配置 RSS） | P2 | 网络 |
| M9 | 桌面小组件 | P1 | 依赖 M2/M5 |
| M0 | 首页聚合 Dashboard | P0 | 依赖以上 |
| — | 设置（含外观、关于） | P1 | 依赖以上全部（提供统一入口） |

---

### 3.2 M1 纪念日 & 经期记录

**经期记录**
- 记录每次月经的开始日期、结束日期（可留空表示进行中）、备注（痛经/量/情绪）。
- 自动计算：平均周期长度（近 6 次）、平均经期天数、下次预计开始日、当前处于第几天 / 距下次还有几天。
- 首页卡片显示：`距下次约 5 天` 或 `经期第 3 天`。
- 提醒：预计开始日前 N 天（默认 2 天，可配置）本地通知；经期中每日可选提醒关怀。
- 日历视图：月历上用色块标出历史经期区间 + 预测区间（预测用浅色/虚线区分）。
- 预测算法（V1 简单可靠）：`下次开始 = 最近一次开始 + round(近 6 次周期均值)`。样本 < 2 次时用默认 28 天，并在 UI 明确标注「样本不足，仅供参考」——不要给出看起来很确定但其实没依据的数字。

**每日异常记录（2026-08-14 新增）**

现状是「一次月经一条记录、一条备注」，粒度太粗——「哪一天分泌物颜色不对」这种事记不下来。
改成**按天**记录，与「一次月经」是两张表、两个生命周期：日记录独立于月经区间存在
（排卵期出血、经期外的异常同样要能记）。

- 入口：经期页月历上点某一天 → 现有弹窗（记为开始 / 记为结束 / 取消）**增加一项「记录今日情况」**。
- 内容 = **预置标签（多选）+ 自由文本（可留空）**：
  - 预置标签分组：分泌物（颜色异常 / 有血丝 / 量多 / 异味）、体感（痛经 / 腰酸 / 乳房胀痛 / 头痛 / 腹胀）、
    情绪（低落 / 易怒 / 焦虑）、其他（同房 / 用药）。标签集合写在代码里，V1 不做用户自定义标签。
  - 标签是**可统计**的那一半（「这个月痛经几天」「连续三个周期都在同一天出血」），
    自由文本是兜底的那一半，不参与统计。两者都要有，只留一个都会漏。
- 有记录的日期在月历上打一个小标记（点/角标），点进去可编辑或删除。
- 记录本身**不参与经期预测算法**（预测只认开始/结束日期），但会作为 AI 分析的输入之一（见 3.14）。

**周期分期：排卵期与黄体期（2026-08-14 新增）**

- 计算口径（**基于「黄体期长度相对固定」这个前提，不是「周期中点」**）：
  - 排卵日 ≈ `下次预计开始日 − 14 天`
  - 易孕窗口 = 排卵日前 5 天 ~ 排卵日后 1 天（精子存活期决定了窗口是往前开的）
  - 黄体期 = 排卵日次日 ~ 下次开始日前一天
  - 卵泡期 = 本次经期结束次日 ~ 排卵日前一天
- **可靠性直接继承经期预测**：分期是从「下次预计开始日」倒推的，预测不可靠时分期只会更不可靠。
  因此 `reliable == false`（间隔样本 < 3 次，见下方落地情况第 3 条）时，
  **月历上不画分期**，只在文案里说明「样本不足，暂不推算排卵期」——画一个没依据的排卵日
  比什么都不画更糟。
- 月历展示：实心块 = 实际经期，虚线圈 = 预测经期，易孕窗口 = 浅色底，排卵日 = 单日强调 + 图标，
  黄体期 = 底部细条。**不能只靠颜色区分**（色觉障碍 + 深色模式下浅色底几乎看不出来），
  每一类都要有形状/图标维度，并在月历下方给图例。
- **明确不做避孕依据**：页面上要有一句「仅供参考，不能作为避孕或备孕的依据」。
  安全期避孕本身就不可靠，排卵日又是推算出来的，这句不是免责套话而是事实。

**纪念日**
- 字段：标题、日期、类型（公历/农历）、重复（一次性/每年）、提前提醒天数、备注。
- 列表按「距今天数」升序，显示 `还有 12 天` / `已过去 300 天`。
- 支持累计型纪念日（如「在一起第 X 天」）。
- 需支持农历（生日常用农历）——用本地农历换算库，不联网。

**验收标准**
- 新增一条经期记录 ≤ 3 次点击完成。
- 关机重启后提醒仍能准时触发（精确闹钟 + 开机广播重建）。

**落地情况（v1.5，已在真机构建通过）**

已实现：纪念日四类倒数（每年 / 累计 / 一次性 / 农历）、置顶、左滑删除 + 撤销；
经期状态卡、月历（实心块 = 实际，虚线圈 = 预测）、水平滑动切月、历史记录列表。
首页两张卡片（纪念日、经期）均通过 `HomeCard` 插件机制注入，`:feature:home` 未改动一行。

实现中做的三个补充决定（PRD 原文没写死，记录在案）：

1. **农历自研，不引三方库**。`:core:common` 里的 `LunarCalendar` 用 1900~2100 查表实现，
   离线、零依赖。正确性由 `LunarCalendarTest` 保证：40 个已知春节日期、13 个已知闰月、
   以及**全范围逐日往返校验**（公历→农历→公历 必须相等，7 万余天）。
   农历生日两个必须容错的边界：当年没有对应闰月时退回平月；生日是三十而当月只有 29 天时落到当月最后一天。
2. **「进行中」设 15 天上限**。忘记点「结束」远比经期真的持续半个月常见，
   不设上限的话一次漏记会让首页永远显示「经期第 87 天」。超限后自动转为等待下一次。
3. **可靠性阈值比 PRD 下限严一档**。PRD 写「样本 < 2 次用默认 28 天」，
   实现里额外要求**至少 3 个间隔**才算 `reliable`——只有 1 个间隔时「平均」就等于那一次本身，
   把单次波动原样当成预测，仍然该提示「样本不足，仅供参考」。
   另外会剔除 10~90 天之外的异常间隔（补录历史时录错年份的典型症状）。

尚未实现：提醒闹钟（等 `ReminderScheduler` 落地，代码里已留 TODO 钩子）、经期备注的编辑入口。

**2026-08-14 已实现**：每日异常记录（`period_day_log` 新表 + 日期弹窗里的「记录今日情况」+
预置标签与自由文本 + 月历右上角标记）、排卵期/黄体期推算与月历展示
（不可靠时不画，状态卡补一行「今天在哪一段」）。设计取舍与口径见 `docs/交接文档.md`
第九节 `:feature:period`。

AI 分析同日落地，见 3.14。

**仍未实现**：经期记录本身的备注编辑入口（`PeriodRepository.update()`
早就写好了但没有 UI 调用，与新增的每日记录是两件事）。

---

### 3.3 M2 待办与日程提醒

- 字段：标题、备注、截止时间（可空）、优先级（高/中/低）、标签、重复规则（每天/每周/每月/自定义间隔）、完成状态。
- 视图：今日、最近 7 天、全部、已完成。
- 完成后重复任务自动生成下一条。
- 提醒：到期时间点本地通知；支持「稍后提醒（15 分钟 / 1 小时 / 明天）」。
- 首页「今日待办」区块显示未完成项，可直接勾选完成。
- **逾期项高亮并置顶**——过期待办若沉在列表底部就等于不存在了。

**权限**：`POST_NOTIFICATIONS` 运行时申请；精确闹钟用 `USE_EXACT_ALARM`（安装即授予，见 9.2）。

---

### 3.4 M3 快速笔记

- 首页悬浮按钮 / 通知栏常驻快捷入口 → 直接进输入框，聚焦即可打字。
- 字段：正文（Markdown 轻语法：标题、列表、加粗、代码块）、标签、创建/更新时间、是否置顶。
- 支持插入图片（Photo Picker 选图，存本地私有目录，数据库存相对路径）。
- 全文搜索（Room FTS），搜索结果高亮命中词。

---

### 3.5 M4 疑问收集箱

定位：与笔记分开——笔记是「已知的沉淀」，疑问是「未解的待办知识」。

- 字段：疑问内容、来源上下文（可空）、标签、状态（待解决 / 已解决 / 已归档）、答案正文（解决时填写）。
- 列表默认按状态分组，`待解决` 优先。
- 解决后可一键「转为笔记」，把疑问 + 答案合成一条笔记存入 M3。
- 首页可选显示「随机一条待解决疑问」，起提醒作用。

---

### 3.6 M5 记账（通知栏自动解析 + 预算）

> 这是「不知如何实现」那条需求的落地方案。安卓上无服务器实现自动记账，唯一可靠路径是 **NotificationListenerService 监听支付类通知并解析**。

#### 3.6.1 自动记账

**工作流程**
1. 设置页开启「自动记账」→ 跳转系统「通知使用权」页面授权。
2. App 后台常驻 `NotificationListenerService`，只处理白名单包名：微信、支付宝、云闪付、各银行 App。
3. 命中白名单后取 `EXTRA_TITLE` + `EXTRA_TEXT`，用**规则引擎**（正则模板列表，存本地 JSON）提取：金额、方向（收/支）、渠道、对方/商户名。
4. 解析成功 → 写入一条 `status = 待确认` 的账目，发通知「已记录 -￥23.50 星巴克，本月剩余 ￥1,842」。
5. 用户点通知或进 App 确认/修改分类，状态转 `已确认`。解析失败的原始文本入「未识别队列」，可手工补全并「保存为新规则」。

**规则示例（内置）**

| 来源 | 文本样例 | 提取 |
|---|---|---|
| 微信支付 | `微信支付凭证 - 向 星巴克 付款 23.50 元` | 支出 23.50 / 星巴克 |
| 支付宝 | `支付成功 ￥23.50 星巴克咖啡` | 支出 23.50 / 星巴克咖啡 |
| 银行卡 | `您尾号1234卡8月5日支出人民币23.50元` | 支出 23.50 / 银行卡 |

**分类体系（已定稿）**

内置一套默认分类，同时**完全可自定义**——两者不是二选一，而是「开箱即用 + 随时改」。

| 默认分类 | 说明 |
|---|---|
| 餐饮 / 交通 / 购物 / 娱乐 / 居住 / 医疗 / 人情 / 学习 / 其他 | 覆盖日常绝大多数支出 |

- 分类**存在数据库表 `category` 里，不是硬编码枚举**。默认集合首次启动时从 assets 里的 JSON 种子写入。
  这一条是刚性的：写成枚举，用户想加一个分类就得改代码 + 做数据库迁移。
- 每个分类可改名称、改图标、改颜色（取自莫兰迪色板，见 5.1）、调排序、停用。
- 停用而非删除：已有账目仍指向它，硬删会让历史统计出现「无主账目」。停用后只是不再出现在选择器里。
- 支持自建子分类（一级够用时不强制展开），最多两级。
- 「未分类」是保留项，不可删除、不可停用——自动记账没命中规则时必须有地方落。

**自动分类**：基于商户名关键词映射表（本地，可编辑），如「星巴克/瑞幸 → 餐饮」「滴滴/高德 → 交通」。命中不到归「未分类」；用户改过一次后记住该商户的分类（学习式映射）。自定义分类同样参与学习式映射。

**手工记账**：始终保留，3 步内完成（金额 → 分类 → 保存）。

**统计**：按日/周/月的收支总额、分类饼图、月度趋势折线、Top 商户。

**已知限制（写明预期，不要指望它 100% 工作）**
- 依赖通知内容，关掉支付 App 的通知就无法记录。
- ColorOS 杀后台会导致监听失效，需引导加白名单（见 9.3）。
- 通知文案改版会让规则失效，因此**必须**保留「未识别队列 + 自定义规则」兜底。
- 不做无障碍服务爬取支付页面——那是隐私性更强、也更脆弱的手段。

#### 3.6.2 预算

**预算周期（已定稿）**

账单周期**起始日默认为每月 10 号（发薪日），可在设置里改成 1~28 中的任意一天**。

- 「本月预算」指的是**从上一个 10 号到下一个 10 号前一天**这段区间，而不是自然月。
  钱是 10 号进账的，按自然月算余额会在月初和月末各错一次，这个数字就没有指导意义了。
- 周期文案统一写成「本期」而不是「本月」，避免和自然月混淆。首页与小组件同步。
- 上限取 28：29~31 号在部分月份不存在，落到 28 之后要额外定义「没有这天怎么办」，
  收益不值得这份复杂度。真发薪在 30 号的话，设 28 号误差只有两天。
- 周期边界一律用 `AppTime` 的本地时区计算并落成 epochMilli 区间，与其他模块的按天查询保持同一套口径。

**预算设置**
- 周期总预算（必选）：设置每个账单周期可支出金额。
- 分类预算（可选）：为任意分类（含自定义分类）各自设上限，分类预算之和不必等于总预算。
- 预算可设为「每期自动沿用上期」或「每期手动确认」。

**每笔支出即时提示（核心体验）**

每记录一笔支出（自动解析成功 或 手工记账保存），立即反馈：

```
┌────────────────────────────────┐
│  ✓ 已记录支出  ￥23.50          │
│  星巴克 · 餐饮                  │
│                                │
│  本期预算剩余  ￥1,842 / ￥3,000  │
│  ▓▓▓▓▓▓▓▓▓░░░░░░░  61%          │
│  距 3 月 10 日还有 12 天         │
│  餐饮分类剩余  ￥318            │
│  按当前节奏，预计到期超支 ￥120  │  ← 仅在预测超支时显示
└────────────────────────────────┘
```

- **自动记账**触发时：以**系统通知**形式提示（App 在后台），内容含「已记录 -￥X，本期剩余 ￥Y」，点击进确认页。
- **手工记账**保存时：App 内浮层提示，2.5 秒自动消失，进度条带动画增长。
- 进度条颜色随消耗比例过渡：`< 70% 正常色 → 70~90% 警示黄 → > 90% 警告红`，颜色切换有过渡动画。
- 若该笔支出命中某个分类预算，同时显示该分类剩余。

**预算预警**

| 触发条件 | 行为 |
|---|---|
| 单期消耗达 80% | 一次性通知提醒 |
| 单期消耗达 100% | 通知「本期预算已用完」，后续每笔提示改为「已超支 ￥X」 |
| 分类预算超支 | 记账提示中该分类标红 |
| 按日均消耗预测到期会超支 | 记账提示与首页卡片显示预测超支金额 |

预测算法：`预计到期支出 = 已支出 ÷ 周期内已过天数 × 周期总天数`。
周期总天数按实际的 10 号到 10 号计算（28~31 天不等），不用固定 30。

**预算视图**
- 独立「预算」页：总预算环形进度 + 各分类横向进度条 + 距下个发薪日剩余天数 + **日均可用余额**（`剩余预算 ÷ 剩余天数`，这个数字比「还剩多少」更有指导意义）。
- 历史周期达成情况回顾（近 12 期柱状图，超支周期标红）。

**首页卡片**：今日支出合计 + 本期预算进度条 + 待确认笔数徽标。

---

### 3.7 M6 知识库

> **2026-08-14 改版**：知识库的主体从「飞书公开网页」换成**打包进 App 的 md 面试题库**。
> 原因是实际使用下来，知识库真正要承载的是「随时抽一道面试题复习」，而飞书方案
> 依赖网络、依赖飞书前端 DOM 不改版、正文提取还经常拿不全，用来支撑每日抽题太脆。
> md 是本地的、结构确定的、离线绝对可用的。飞书降级为「可收藏、可 WebView 打开」的只读书签。

#### 3.7.1 md 面试题库（主力）

**内容来源**：md 文档随 APK 打包在 `assets/interview/<doc>/`，图片放同目录的 `img/`。
当前内置两篇：`backend`（后端，14 章 289 题）、`llm`（大模型，17 章 205 题），合计 494 题。

**文档结构约定**（解析器按此切分）：
- `# 一、基础篇` = 章节
- `## 什么是多态` = 一道题，标题是题干，到下一个 `##` / `#` 之间的正文是答案
- `###` 及更深的标题留在答案正文里，不单独成题
- **代码围栏（``` / ~~~）内的一切都不参与标题识别**——两篇文档里有几十处
  `# 注释` 写在 Python/Shell 代码块里，裸按行首 `#` 判会凭空多出十几个假章节、还会把题目截断

**功能**
- **章节列表**：按文档分组，每章显示题数 + 「是否参与每日抽题」开关，可整篇一键全选/全不选。
- **题目阅读**：markdown 渲染（加粗、行内代码、列表、引用、代码块横向滚动、图片），
  底部「已掌握 / 再看看」反馈直接影响复习调度。
- **导入**：App 启动时按资源版本号（`INTERVIEW_ASSETS_VERSION`）决定是否重新解析。
  重新导入是整篇替换，但**章节的在池状态与复习进度按稳定 key 保留**，不会被重置。

**扩展**：加一篇新题库 = 放 md + 在 `INTERVIEW_DOCS` 加一行 + 版本号加一，不动其它代码。

#### 3.7.2 飞书公开网页（降级为只读书签）

**接入方式**：知识库是**公开分享到互联网的飞书网页**，因此**不需要 OAuth、不需要自建飞书应用、不需要 App Secret**——原方案中最大的安全风险与最重的接入成本直接消除。

**改版后的定位**：仍可收藏、分组、WebView 阅读、全文搜索已缓存的正文，
但**不再参与每日抽题**，正文提取链路（无头 WebView + JS 注入）随之停用——
那条链路本来就是为抽题提供摘要的。入口从「知识」tab 顶栏的齿轮进。

**功能**
- **知识源管理页**：手动添加飞书公开链接（`https://xxx.feishu.cn/docx/...` / `/wiki/...` / `/base/...`），可命名、分组、排序、启停、删除。支持从系统分享菜单「分享到 MyAPP」直接添加。
- **阅读**：内嵌 WebView 打开，保留飞书原生排版；顶部自定义工具栏（返回、刷新、外部浏览器打开、加入知识池、复制链接）。
- **正文提取与缓存**：后台在无头 WebView 中加载页面，渲染完成后注入 JS 抽取标题与正文纯文本，存入本地缓存，用于：① 摘要展示 ② 全文搜索 ③ 断网时的降级阅读。
- **离线**：已缓存页面断网可读纯文本版，标注「缓存于 X 时间」；未缓存则提示无网络。
- **搜索**：对已缓存正文做本地全文搜索（FTS），命中后跳转对应页面。
- **收藏/置顶**：常用页面置顶，首页可配快捷入口。

**技术要点与限制（必须知晓）**
- 飞书文档页是 JS 渲染的 SPA，**直接 HTTP 抓 HTML 拿不到正文**。正文提取必须走 WebView 渲染后注入 JS，不能用 Jsoup 之类的静态解析。
- 提取逻辑依赖飞书前端 DOM 结构，飞书改版可能导致失效。因此：**提取失败不影响阅读**（WebView 始终可用），只影响摘要与搜索；提取选择器存本地配置，可在 App 内调整而无需发版。
- 页面必须保持「互联网公开」分享状态。一旦改为需登录，App 检测到后在源列表标记「需要登录，已失效」。
- 正文提取任务放后台低优先级执行（充电 + WiFi 优先），避免影响流畅度与耗电。

**扩展预留**：`KnowledgeSource` 接口保持不变（见 4.7.4），当前实现为公开网页版；将来若需要私有飞书文档，再补一个 OAuth 实现即可，上层无感知。

---

### 3.8 M7 知识点每日推送

- **候选集是 md 面试题库里「在池章节」下的全部题目**（3.7.1 改版后；改版前是飞书知识源）。
  知识池的开关粒度是**章节**——500 道题一条条勾不现实，而「这阵子只复习 Redis 和 Spring」是真需求。
- 每天挑选一道题：优先没推过的（按原书顺序，读起来有连贯性）；
  全推过后按间隔复习权重排序（1/3/7/15/30 天），取最逾期的一道。
- 首页「今日知识点」卡片显示题干 + 答案摘要（**纯文本，去掉 markdown 标记与转义**）+ 「文档 · 章节」，
  点击「看完整答案」进题目阅读页。
- 「已掌握 / 再看看」反馈按钮影响复习权重（已掌握进一档、封顶后原地循环；再看看回到第 0 档），
  卡片带翻转/滑出动画。首页卡片与题目阅读页底部都有这组按钮——
  从卡片跳进去看完整答案后往往才知道自己会不会，只在卡片上给按钮不够用。
- 可选每日定时通知推送（08:00，AlarmManager 自续期，不用 WorkManager，见 9.3）。

**降级**：题库为空（还没导入）或所有章节都被移出池时，从本地笔记（M3）中随机挑选，保证卡片永不空白。

**未做**：按 H1/H2 把长文切成多个知识块。md 题库天然就是「一题一块」，
这条原本是为飞书长网页设计的，改版后不再需要。

---

### 3.9 M8 行业新闻（自配置 RSS）

- 订阅源管理页：手动添加 RSS/Atom 地址、命名、分组、启用/禁用、排序、删除。支持 OPML 导入导出。
- 后台定时（默认每 2 小时，可配置；可限仅 WiFi）拉取各源，端上解析 XML，去重（按 guid/link）后入库。
- 列表：全部 / 按分组 / 未读 / 已收藏 / **按订阅源**。显示标题、来源、时间、摘要、封面图（若有）。
  筛选条横向滚动（订阅源可能十几个，换行会吃掉半屏）。
- **列表必须分页且不查正文**：首屏 50 条、滚到底再加 50。
  2026-08-14 实测，无 LIMIT 且查整行（含 `content`）时，5469 篇文章要一次性读出近 8MB 正文，
  每次进资讯页都卡好几秒，而列表根本用不到正文。改用不含 `content` 的投影 + LIMIT 后即时打开。
- 详情：优先展示 RSS 正文；无正文则 Custom Tabs 打开原链接。
- 操作：已读、收藏（收藏项永久保留）、分享链接、「存为笔记」（一键把标题+链接+摘要存入 M3）。
- 清理策略：非收藏文章保留 30 天，超期自动清理，控制数据库体积。
- 首页显示最新 3 条未读。

---

### 3.10 M9 桌面小组件

用 **Jetpack Glance**（Compose 风格的 AppWidget 框架）实现，与主 App 共用 Repository 层。

**W1 · 今日概览（4×2）**
```
┌──────────────────────────────────────┐
│  8月5日 周三                     ⊕   │
│                                      │
│  今日支出   ￥86.50                   │
│  预算剩余   ￥1,842  ▓▓▓▓▓▓░░░░ 61%   │
│  ──────────────────────────────────  │
│  ○ 交周报                      18:00 │
│  ○ 买牛奶                            │
│  ○ 预约体检                          │
│                        还有 2 项 →   │
└──────────────────────────────────────┘
```
- 上半区：今日支出合计 + 本期预算剩余与进度条（超支时转警告色）。
- 下半区：今日未完成待办前 3 项，**圆圈可直接点击打勾完成**（`actionRunCallback` 直接写库，无需打开 App）。
- 右上 ⊕ 快捷记一笔，点击标题区打开 App 首页。
- 待办为空时显示「今天没有安排 🎉」。

**W2 · 今日支出（2×2）**
```
┌──────────────────┐
│  今日支出         │
│  ￥86.50          │
│      ╭───╮       │
│      │61%│       │  ← 环形进度，色随消耗比例
│      ╰───╯       │
│  本期剩余 ￥1,842 │
└──────────────────┘
```
金额用衬线体 + 表格数字（见 5.2），点击进记账页，长按进快捷记一笔。

**W3 · 今日待办（2×2）**：今日未完成前 4 项，圆圈可直接勾选。全部完成时整块显示一个完成态图形而不是空白。

**W4 · 纪念日倒数（2×2）**
```
┌──────────────────┐
│  在一起           │
│                  │
│     1 024        │  ← 大号衬线数字
│       天          │
│                  │
│  下一个 · 生日 12天│
└──────────────────┘
```
- 显示最近一个纪念日的倒数 / 已过天数，底部一行带出下一个即将到来的。
- 这是「给女朋友记录」这条原始需求里唯一适合常驻桌面的信息——
  经期数据放桌面有隐私风险（任何人扫一眼手机都能看到），**明确不做经期小组件**。
- 可在小组件配置页选择要盯哪一个纪念日。**未配置时的默认对象按下面这条链取**（v1.5 定稿）：

  | 优先级 | 取谁 | 为什么 |
  |---|---|---|
  | 1 | 用户**置顶**的那条 | 显式意图优先于任何推断 |
  | 2 | 距今天**最近的下一个**（不分类型） | 倒数小组件的价值就在「快到了」 |
  | 3 | **最早创建**的那条 | 保底。只按「下一次」排，会让一个只剩历史纪念日的用户看到空白小组件——空白会被当成故障 |
  | 4 | 一条都没有 → **引导卡片**「点击添加第一个纪念日」 | 空态绝不留白屏（5.4） |

  置顶是**单选**：设置新的置顶会自动取消旧的，避免出现两个「默认」。

**尺寸与配置**
- 四个小组件都声明 `targetCellWidth/Height` + `minWidth/Height` 双份，兼容新旧启动器网格。
- 支持 `ACTION_APPWIDGET_CONFIGURE`：W4 选纪念日，W1 选是否显示支出区（不想让人看到钱的场景）。

**刷新策略**
- 数据变更驱动：新增账目、完成待办、修改预算时主动 `updateAll()`，保证即时。
- 兜底周期刷新：每 30 分钟一次，以及每日 0 点跨天刷新。
- **小组件绝不做网络请求**，只读本地 Room，保证秒开不卡。

**视觉**

跟随 App 主题（浅/深色自动切换）；圆角与留白与主 App 一致。使用小组件预览生成 API（`setWidgetPreview`），让小组件在系统选择器里显示**真实数据预览**而非静态截图。

小组件是桌面上的一块，不是 App 的缩略图，因此有三条额外约束：

1. **不画 1px 描边。** App 内用描边表达层级（5.3），但小组件本身就是一块独立卡片，
   再加一圈边会变成「框中框」。层级只靠底色和留白。
2. **整块只用一个强调色。** 桌面上壁纸颜色不可控，多色会立刻显脏。
   进度条、倒数数字、勾选态共用同一个 Accent，其余全部走中性色。
3. **不放图标装饰。** 每个像素都留给数据本身——桌面上人只看一眼。

**验收**：数据变更到界面更新 < 1s；勾选待办后小组件与 App 内状态一致。

---

### 3.11 M0 首页 Dashboard

单页竖向滚动的卡片流，卡片顺序与显隐可在设置中调整：

1. **日期问候** + 纪念日倒数最近一项
2. **今日待办**（未完成项，可直接勾选，空态显示「今天没有安排 🎉」）
3. **经期状态**（距下次 / 第几天 + 快捷「记录开始」按钮）
4. **今日知识点**
5. **今日记账**（今日支出 + 预算进度 + 待确认笔数徽标）
6. **行业动态**（3 条）
7. **一条待解决疑问**

底部导航：`首页 | 记录（笔记/疑问） | 记账 | 资讯 | 我的`
全局悬浮快捷：长按 FAB 展开 → 记笔记 / 记疑问 / 记一笔 / 加待办。

---

### 3.12 设置

设置页汇总各模块的配置入口，本身不是一个独立业务模块。已在其他章节定义并落地的入口——自动记账开关与规则管理（3.6）、分类管理（3.6）、首页卡片排序（3.11）、经期提醒提前天数（3.2）、保活自检（9.3）——不在本节重复。本节补齐此前 PRD 未写明、导致实现中长期是禁用占位的两项：**外观**、**关于**。

**外观**
- 主题模式：跟随系统 / 浅色 / 深色，三选一，默认跟随系统；切换即时生效，沿用 6.3 已定的「深浅色切换全局 300ms 颜色过渡」动效参数，不新增规范。
- 动态取色（Material You）：可选项，默认关闭——5 章已明确「默认保持品牌感」是刻意的设计决定，本条只是把它接到设置页的开关上。
- 动效强度：完整 / 精简 / 关闭 三档（6.3 已提到入口在设置页，此前缺具体交互定义）。「完整」= 当前 `MotionScheme.expressive()` 全量效果；「精简」= 缩短时长与位移量，保留必要的状态反馈；「关闭」= 只保留瞬时状态切换，去掉所有非必要动画。系统「移除动画」无障碍开关打开时，本项强制锁定为「关闭」并禁用手动切换（呼应 6.4 已有的无障碍规则）。
- 存储：`core/datastore` `AppPreferences` 新增 `themeMode` / `dynamicColorEnabled` / `motionIntensity` 三个偏好，与已有的 `periodReminderLeadDays` 同一套存取方式。

**关于**
- 版本信息：应用版本号 + versionCode（读 `BuildConfig`）。
- 数据管理入口：「导出全部数据」「导入」快捷跳转，复用 4.6 已定义的备份能力——关于页只是另一处入口，不重复实现。
- 开源许可：第三方依赖许可证列表（技术选型留给实现时决定，如 AboutLibraries 或手工维护清单）。
- 不做「检查更新」：呼应第一节「单人自用，不上架」硬约束，新版本由开发者手动安装 APK，没有更新检查的必要性。

---

### 3.13 云数据备份

**背景**：4.6 定义的本地导出/导入（加密 zip + SAF）是换机的唯一路径，但依赖「记得手动导出」——现实中大概率会忘。本节新增一条**可选**的云端通道：定期/手动把同一份备份快照上传到已单独开通的腾讯云开发（CloudBase）账号，作为本地导出的补充，而不是替代。**参考实现代码见 `docs/云数据备份.md`（腾讯云开发账号信息、鉴权与 PostgreSQL REST API 调用示例）。**

> ✅ **凭证已移出可提交文件**：账号手机号/邮箱/密码已迁到 `docs/云数据备份.local.md`（`.gitignore` 已加 `*.local.md` 规则），`docs/云数据备份.md` 只留接入方式说明。App 侧实现时仍需遵守同一原则：登录成功后本地只落地 `access_token`/`refresh_token`（加密存储），不落地密码明文。

**定位与非目标**
- 这是「单人自用」场景下的**私人备份盘**，不是同步引擎：不做多设备实时同步、不做冲突合并、不做增量/差量同步。V1 是「整份快照上传 / 整份快照下载覆盖」，语义与本地导入「覆盖恢复」完全一致，复用同一套序列化与恢复代码路径。
- 云端只存**这一个人的备份数据**（对应用户的原话「数据仅存储自己的东西」）：腾讯云开发账号是专门为这个 App 单独开通的，不与其他项目/其他人共用；备份表结构里也不设计任何多用户/多租户字段（不加 `owner_id` 之类的区分列——因为从设计上就只有一份数据，加了反而是没用的复杂度）。
- V1 备份范围 = **结构化数据**（Room 表，序列化方式复用 4.6 已定义的导出格式）。笔记等模块里的图片/附件**不**随云备份上传——原因：CloudBase 这套 PostgreSQL REST API 是给结构化行数据用的，图片会让快照体积从「几十 KB 文本」跳到「数 MB～数十 MB」，V1 先用最小可行范围验证链路可靠性，图片仍只能通过本地导出/导入迁移。
- **`rss_article` 也排除在外**（落地时实测后确定）：它一张表就占了整份备份的 99.99%——5469 篇文章的正文与摘要合计近 8MB，而其余 11 张表的全部个人数据加起来只有几百字节；而这 5469 篇里只有 1 篇被收藏、12 篇标了已读。资讯正文是从 RSS 源抓来的外部内容，恢复后后台任务会重新抓，没有理由每天上传一次。订阅源本身（`rss_source`）是用户配置，仍然备份。实测效果：单份备份从 4.2MB 降到 15KB。
  - 恢复时仍要**清空**本机 `rss_article`：`rss_source` 是整表覆盖的，留着旧文章会让它们的 `source_id` 指向对不上的订阅源。
  - 三张 FTS 表（`note_fts` / `question_fts` / `knowledge_content_fts`）同样不备份，它们由 Room 触发器从主表重建。
- 不做账号自助注册/找回密码等产品化账号体系——账号是运维层面手工开一个，App 里只做「登录一次、长期持有 token」。

**功能点**
- 设置页新增「云备份」入口（挂在 3.12 设置页，`关于` 卡片旁新增一张卡片，而不是塞进关于页），三种状态：
  - **未登录**：引导填手机号/邮箱 + 密码登录一次（对应 `docs/云数据备份.md` 的 `/auth/v1/signin`），成功后本地只留 `access_token`/`refresh_token`（加密存储），不落地保存密码。
  - **已登录，从未备份 / 有历史备份**：展示「上次备份时间」「备份大小」「立即备份」按钮 + 最近几次备份的历史列表（时间、大小、App 版本号），可从历史列表中选一条恢复，也可删除过旧的备份。
  - **恢复中/备份中**：进度态 + 失败态（网络错误/鉴权过期/数据库版本不兼容需给出可读文案，不是裸异常堆栈）。
- 备份触发方式：手动「立即备份」为 V1 必须实现；自动定时备份（如每日一次，WiFi + 充电，走 WorkManager）作为可选增强，具体是否 V1 一起做由实现阶段决定——不管哪种触发方式，都复用同一条备份流水线，只是触发源不同。
- 恢复流程与本地「导入覆盖恢复」共享同一套「确认覆盖」二次确认交互（同一场景，不做视觉/交互上的第二套设计）。
- 登录态过期（token 失效）时，云备份卡片自动降级为「未登录」态，不能静默失败让用户以为数据仍在正常备份。

**技术方案要点**（供 4.6/4.7.4 落地时参考，细节以实现阶段为准）
- 鉴权（落地时已实测）：`POST /auth/v1/signin`，`username` 用**昵称或邮箱**（手机号会被拒），返回的 `access_token` **有效期 24 小时**。由于每日备份是无人值守任务，token 每次醒来基本都过期了，因此采取「用 Keystore 里存的账号密码重新登录」而不是维护 refresh_token——密码本来就必须存（否则任务无法自愈），多存一个 refresh_token 没有收益。只有密码本身失效时才退回未登录态。
- 存储模型：不做「每张 Room 表对应一张远程表」的逐表镜像同步——Room 的表结构随迁移持续变化，逐表镜像意味着每次 `Migration` 都要同步改远程 schema，维护成本和出错概率都高，且与「无云端账号体系」的定位不符。改为**单张远程表存整份快照**：
  ```
  表：app_backup
  列：id（主键）、created_at、app_version、db_schema_version、size_bytes、checksum、payload
  ```
  `payload` 是 4.6 已定义的「加密 zip 全量导出」内容（Base64 编码后写入）；恢复时按 `created_at` 倒序取最新一条（或用户在历史列表里选中的一条），Base64 解码 + 解密 + 走本地「导入覆盖恢复」的既有代码路径。这样云备份本质上只是「本地导出/导入」的一个新目的地，不是独立实现一遍。
- 加密：复用 4.6 「导出全部数据」已定义的加密方案，云端存储的是密文，腾讯云开发侧拿不到明文（即便账号泄露，备份内容本身仍受本地设定的密码保护）。
- 落地位置：备份引擎在 `feature/settings/backup/`，整库导出/恢复在 `core/database/backup/`（`withTransaction` 在那儿）。未实现 `SyncProvider` 接口——那个接口的 `push()/pull()` 无参签名装不下「选哪一份备份恢复」「备份密码」这些必需信息，硬套只会得到一个假抽象；等真有第二个同步后端时再按实际需要重新定义（4.7.4 的表格已相应更新）。

---

### 3.14 AI 能力（DeepSeek，2026-08-14 新增）

**背景与边界**：这是全 App 第二个联网的第三方依赖（第一个是 3.13 云备份）。
核心约束「无自建服务器、本地优先」不变：**AI 只做锦上添花的解读，不做任何本地算法做不到的事**。
经期预测、分期推算、提醒全部由本地算法负责，AI 挂了、没网、峰价时段，功能照常。

**V1 用途（只做经期这一处，不铺开）**
- 结合历史周期与每日记录，给一段**可读的解读**：节律是否稳定、最近的异常记录是否值得留意、
  下个周期大概什么时候、日常建议。
- 明确**不是医疗诊断**。回答里出现「疑似/建议就医」这类内容时，措辞是「建议找医生看看」，
  不给具体病名与用药建议。页面固定展示一句「AI 生成，仅供参考，不能替代医生」。

**接口口径**
- 模型：**DeepSeek V4 Flash**，调用时通过 `tools` 参数传 `web_search` 启用**联网搜索**
  （2026-08-14 由用户核对官方文档确认）。模型 id 与是否联网仍写成配置项——
  换模型/临时关联网省钱时不该改代码。
- 调用方式：**Responses API**（`POST https://api.deepseek.com/responses`），复用 `:core:network`
  的 OkHttp/Json 单例。**不是**对话补全接口——2026-08-14 实现时查官方文档发现，
  `/chat/completions` 的 `tools` 只支持自定义 function，服务端执行的内置 `web_search`
  只有 Responses API 有。联网搜索是这个功能的硬需求，因此接口选型跟着它走。
- **联网搜索会显著拉长响应时间**，UI 必须有明确的进行中态（不是转圈就完事，
  要能说明「正在联网查资料」），且超时按下面的 30s 算。
- 超时与失败：单次请求超时 30s，失败不重试（用户正看着，等第二次不如让他自己再点一下），
  失败文案要区分「没网」「鉴权失败」「峰价被挡」「模型返回异常」四种。

**密钥管理**
- API Key **不写进任何可提交文件**，与云备份账号同一条规矩：`docs/DeepSeek.local.md`
  （`.gitignore` 已有 `*.local.md`），App 内走加密存储（`SecretStore`）。
- 代码里不硬编码 key；设置页提供填写入口，key 可替换、可清空。

**峰谷定价与可用性**
- DeepSeek 按时段计价（谷时便宜、峰时贵）。**峰价时段 AI 入口置灰并标注「当前为峰价时段」，
  但允许二次确认后强制发起**——白天几乎全是峰价，硬禁用等于这功能白天完全不能用。
- 峰谷时段边界写成配置常量（含时区），不散落在 UI 里；官方调整时段时只改一处。
- 置灰状态下也要能看到上一次的分析结果（结果本地缓存），不能因为峰价连历史都看不了。

**数据范围（用户明确选择：全部）**
- 发送内容 = 周期统计（周期长度、经期天数、近几次间隔）+ **每日异常记录的标签与自由文本** + 经期备注。
- 因此必须有一次**明确的知情同意**：AI 功能默认**关闭**，首次开启时用一句话说清
  「你的经期记录（含你写的文字）会发送到 DeepSeek 的服务器」，用户点了才算数；
  设置页可随时关闭，关闭后不再发送。
- 只发**当前需要分析的那一段**（近 6 个周期 + 相关日记录），不是整库；不发姓名、设备号等身份信息。
- 请求与响应都不落盘存原文，只缓存最终的分析结果文本。

**触发方式**
- 手动触发为主：经期页一个「AI 分析」按钮。**不做自动定时调用**——那会在用户不知情的时候
  持续把私密数据发出去，也会在峰价时段悄悄产生费用。
- 同一份数据（周期数与日记录都没变）不重复调用，直接给缓存结果。

**落地位置（建议）**
- `:core:network` 放 HTTP 客户端与 DTO；`:feature:period` 放业务编排与 UI。
  AI 只服务经期这一个 feature，先不抽 `:core:ai`——等第二个 feature 也要用时再抽，
  否则会得到一个只有一个使用者的假抽象（同 3.13 里 `SyncProvider` 的教训）。

**落地情况（2026-08-14 已实现）**

按上面的建议位置落地，没抽 `:core:ai`：

| 文件 | 职责 |
|---|---|
| `:core:common` `security/SecretStore.kt` | 从 `:feature:settings` **挪到 core 层**——写入方是设置页、读取方是 DeepSeek 客户端，两个 feature 不许互相依赖。`PREFS_NAME`/`KEY_ALIAS` 保持原值，已存的云账号不受影响 |
| `:core:network` `deepseek/DeepSeekClient.kt` | 只管发请求收文字。key 现取现用不缓存成字段；不重试；`callTimeout` 30s（不是 `readTimeout`，后者管的是包间隔，流式慢吐不触发）；失败分 5 类且**文案里不出现 key** |
| `:core:network` `deepseek/DeepSeekPricing.kt` | 峰谷时段判定 + 「几点转谷价」。窗口是 UTC 小时的左闭右开区间，只此一处 |
| `:feature:period` `data/PeriodAiPrompt.kt` | 纯函数：组 prompt + 算输入指纹。**发出去的内容长什么样由它的单测钉死** |
| `:feature:period` `data/PeriodAiRepository.kt` | 编排：没开启不发、指纹命中给缓存、只缓存结果文本 |
| `:feature:period` `ui/PeriodAiCard.kt` | 经期页的分析区，四种状态（未开启/缺 key/峰价/正常）共用一张卡片 |
| `:feature:settings` `ai/AiSettingsScreen.kt` | 总开关（开启前强制过知情同意弹窗）、联网开关、key 的填写/替换/清空 |

与本节原文的两处偏差：

1. **接口从对话补全改成 Responses API**，理由见上面「接口口径」。
2. **模型 id 是代码常量而不是用户可配项**。本节写「模型 id 与是否联网仍写成配置项」，
   实际只有「是否联网」做成了设置页开关；模型 id 收敛到 `DeepSeekClient.MODEL` 一个常量。
   给自用 App 加一个「随便填模型名」的输入框，填错的代价（一次 400 + 一头雾水）
   高于它省下的那次改代码。

峰价时段的处理与本节要求一致：入口不禁用，点下去先弹一个说明费用翻倍、几点转谷价的
二次确认；缓存结果的展示完全独立于可用性判断，峰价时照样看得到上次的分析。

---

## 4. 技术方案

### 4.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Kotlin | — |
| UI | Jetpack Compose + Material 3 | 单 Activity + Navigation Compose |
| 架构 | MVVM + Repository，单向数据流 | ViewModel 暴露 `StateFlow`，UI 无状态 |
| 本地库 | Room（含 FTS 全文搜索） | 唯一真相来源 |
| 异步 | Coroutines + Flow | 所有 IO 走注入的 Dispatcher |
| DI | Hilt | 含 `@IntoSet` 多绑定，支撑首页插件机制 |
| 网络 | OkHttp + Retrofit + kotlinx.serialization | RSS 拉取 |
| RSS 解析 | XmlPullParser（系统自带） | 避免重依赖 |
| 网页阅读 | WebView（内嵌 + 无头提取） | 飞书公开页 |
| 后台任务 | WorkManager（可延迟任务）+ AlarmManager（精确提醒） | 分工见 4.3 |
| 小组件 | Jetpack Glance | 与 App 共用 Repository |
| 图片 | Coil | — |
| 偏好存储 | DataStore | — |
| 动效底座 | Material 3 Expressive（MotionScheme + 形状变形） | 只取动效引擎，不取默认配色，见 9.2.1 |
| 触感 | 系统振动 API（Android 16 增强包络） | 关键动效配同步触感 |
| 构建 | Gradle 版本目录 + build-logic 约定插件 | 见 4.8 |
| 版本 | **minSdk 35 / targetSdk 36 / compileSdk 36** | 见 9.2 |

### 4.2 数据模型（Room 表）

```
period_record       (id, uuid, start_date, end_date?, note,
                     created_at, updated_at, deleted_at?)          -- 日期存 epochDay
anniversary         (id, uuid, title, date, is_lunar, repeat_type, remind_days_before,
                     note?, pinned, created_at, updated_at, deleted_at?)
todo                (id, uuid, title, note, due_at?, priority, tags, repeat_rule?,
                     done, done_at?, created_at, updated_at, deleted_at?)
note                (id, uuid, content, tags, images_json, pinned, created_at, updated_at)
note_fts            (FTS 虚表，映射 note.content)
question            (id, uuid, content, context?, tags, status, answer?, created_at, resolved_at?)

transaction_record  (id, uuid, amount, direction, category, merchant, channel, occurred_at,
                     status /*pending|confirmed*/, raw_text?, source /*auto|manual*/, ...)
budget              (id, cycle_start_day, total_amount, effective_from, effective_to?, auto_rollover)
budget_category     (budget_id, category, amount)
merchant_category_map (merchant_key, category)        -- 学习式分类
parse_rule          (id, package_name, regex, field_map_json, enabled, priority)

knowledge_source    (id, url, title, group_name, enabled, sort_order, pinned,
                     in_pool, last_fetch_at, fetch_status)
knowledge_content   (source_id, section_index, section_title, content_text, fetched_at)
knowledge_content_fts (FTS 虚表)
knowledge_review    (source_id, section_index, last_shown_at, shown_count, mastery_level)
                    -- 飞书退出抽题后停止增长，保留仅为不动已有数据

-- md 面试题库（3.7.1，v12 新增）。三张表都是从 assets 的 md 派生出来的，
-- 没有用户手填内容，所以不做软删除、重新导入时整篇替换。
-- chapter_key / question_key 是稳定标识（sha1），跨重新导入不变——
-- 靠它们保住「章节是否在池」与复习进度，不能用自增 id（重导会变）。
-- 列名避开 SQL 保留字 KEY。
interview_chapter   (id, chapter_key, doc_key, doc_name, title, sort_order, in_pool, updated_at)
interview_question  (id, question_key, chapter_id, title, body, sort_order, updated_at)
interview_review    (id, question_key, interval_level, next_due_at, last_shown_at, updated_at)

rss_feed            (id, url, title, group_name, enabled, sort_order, last_fetch_at)
rss_article         (id, feed_id, guid, title, summary, content?, link, cover?,
                     published_at, is_read, is_starred)
```

**全局字段约定**（为将来同步预留，现在就要有，后补代价极大）：
- `uuid`：全局唯一标识，跨设备同步的前提
- `created_at` / `updated_at`：审计与冲突解决基础
- `deleted_at`：需要软删除的表必须有——同步需要 tombstone，硬删除会导致删除操作无法传播

**日期 vs 时刻**：只有「日」语义的列（纪念日、经期起止）存 **epochDay**，
不存 epochMilli。存毫秒会带上一个假的时刻，跨时区或夏令时时可能整体偏一天。
有真实时刻的列（待办截止时间、各种 `*_at`）才存 epochMilli。

**当前版本**：数据库 v2（v1 = todo，v2 加入 anniversary 与 period_record）。
迁移写在 `core/database/Migrations.kt`，SQL 与 `schemas/2.json` 里 Room 生成的建表语句逐字符比对过。

**索引**：所有按时间/日期查询的列建索引；`rss_article.guid` 唯一索引用于去重。

**迁移纪律**：本项目数据**无云端备份，丢一次就永久没了**。因此严禁 `fallbackToDestructiveMigration()`；每次改表必须写 Migration 并提交 `schemas/` 下的 JSON；迁移要有测试。

**金额存储**：一律用「分」存 `Long`，绝不用 `Float`/`Double`——浮点累加误差在记账场景是不可接受的。

### 4.3 后台任务分工

| 任务 | 机制 | 频率 |
|---|---|---|
| 待办/纪念日/经期提醒 | **AlarmManager 精确闹钟** | 逐条注册，开机广播重建 |
| 支付通知解析 | NotificationListenerService | 事件驱动 |
| RSS 拉取 | WorkManager 周期 | 默认 2h，需网络 |
| 知识池正文刷新 | WorkManager 周期 | 每日一次，WiFi + 充电 |
| 每日知识点选取 + 通知 | WorkManager 定时 | 每日 08:00 |
| RSS 旧数据清理 | WorkManager | 每周一次 |
| 小组件刷新 | 数据变更主动触发 + 周期兜底 | 见 3.10 |

> **关键分工原则**：定时提醒**一律用 `setExactAndAllowWhileIdle`**，不用 WorkManager。ColorOS 会冻结后台，WorkManager 的周期任务在这台机器上不可靠（见 9.3）。WorkManager 只用于「晚一点执行也无所谓」的任务。

### 4.4 权限清单

| 权限 | 用途 | 时机 |
|---|---|---|
| `INTERNET` / `ACCESS_NETWORK_STATE` | RSS + 网页 | 安装即有 |
| `POST_NOTIFICATIONS` | 所有提醒 | 首启引导 |
| `USE_EXACT_ALARM` | 精确提醒 | **安装即授予，无需引导**（见 9.2） |
| `SCHEDULE_EXACT_ALARM` | 精确提醒兜底 | 保留声明 |
| `RECEIVE_BOOT_COMPLETED` | 重启后重建闹钟 | 安装即有 |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 引导加电池白名单 | 保活自检时 |
| `VIBRATE` | 动效触感反馈 | 安装即有 |
| 通知使用权（特殊权限） | 自动记账 | 用户开启该功能时 |
| ~~`READ_MEDIA_IMAGES`~~ | 笔记插图 | **不需要**——用 Photo Picker，免权限 |

### 4.5 性能要求

- 冷启动到首页可交互 **< 800ms**（Baseline Profile + 延迟初始化非首屏依赖）。
- 首页每张卡片独立 Flow，任一模块加载慢不阻塞整页；卡片先出**骨架屏**（不用转圈，转圈会让首页看起来在卡）。
- 单张卡片的数据源异常只让那张卡片显示错误态，**不能拖垮整页**。
- 列表统一 `LazyColumn` + 稳定 key + 分页（RSS 与账目用 Paging），144Hz 下稳定不掉帧。
- 严禁主线程 IO（debug 下开 StrictMode 检查）。
- Release 包开 R8 + 资源压缩，目标 APK < 15MB。

### 4.6 数据安全与备份

- 全部数据在 App 私有目录，不申请外部存储读写。
- **关闭系统云备份与设备迁移**（数据均为隐私内容，且迁移路径是应用内导出）。
- 备份：设置页「本地备份」→「导出全部数据」生成加密归档（Room 全库快照 + 笔记图片）到用户选定位置（SAF）；「从备份文件恢复」覆盖恢复。**这是本地换机的基础路径**，也是**笔记图片唯一的迁移路径**（云备份 3.13 有意不含图片），必须在首启引导里强调定期导出。

  **已落地（2026-08-14）**，实现要点：
  - 归档 = **先 zip 再整体加密**：`manifest.json`（格式版本/App 版本/schema 版本/条数）+ `snapshot.json`（与云备份同一个 `DatabaseSnapshot`）+ `files/notes/<uuid>/<图片>`。整体加密而不是 zip 内逐条目加密，一是避免每张图各推一次 PBKDF2，二是把**文件名本身**（有几张图、笔记 uuid）也一起加密——那也是隐私。
  - 加密复用 3.13 的 AES-256-GCM + PBKDF2(210k)，但换成**分帧**格式（每帧 512KB 独立封装，帧序号与末帧标记进 AAD）：备份要带图片，可能几十上百 MB，单块加解密会把整份数据顶在内存里；分帧后内存恒定，且**文件被截断一定报错**（缺末帧），不会解出「少了一截」的备份。
  - 恢复顺序：整份归档先读完并落进 `filesDir/.restore-*` 暂存目录 → 再事务内覆盖数据库 → 最后才替换 `filesDir/notes`。中途失败（密码不对/文件截断/schema 太新）本机数据保持原样。
  - 归档条目名按不可信输入处理（Zip Slip 防护）：备份文件会在设备之间传递，解出的路径必须限制在暂存目录内。
  - **备份密码不落盘**：它是「这个文件的密码」，与云备份存在 `SecretStore` 里的长期密码不是一回事；忘了没有找回途径，页面上明说。
- 可选云备份（3.13）：上述加密 zip 额外可上传到第三方 BaaS（腾讯云开发），作为忘记手动导出时的兜底；默认关闭，登录一次后可手动/定时触发，云端只存密文，恢复流程与本地导入共用同一套覆盖恢复代码路径。
- 可选：应用锁（生物识别 / PIN）保护进入。

### 4.7 可扩展性设计（重点）

目标：**新增一个功能模块 = 新建一个 module + 注册一次，不改动任何已有模块的代码。**

#### 4.7.1 模块化工程结构

```
:app                        组装层。唯一知道「所有 feature 都存在」的地方
:core:common                时间/协程扩展、Result 封装、跨模块协作契约
:core:designsystem          色彩、字体、形状、MotionTokens、通用组件
:core:database              Room 实例、实体、DAO、迁移
:core:datastore             偏好设置、功能开关
:core:network               OkHttp/Retrofit 基建
:core:ui                    导航契约 Route、首页卡片插槽 HomeCard
:feature:home               首页：只做卡片编排，不认识具体业务
:feature:todo               待办（同时是新增模块的参考实现）
:feature:period / anniversary / note / question / ledger / knowledge / feed / widget / settings
```

**依赖规则（不可违反）**
1. `:feature:*` **之间永远不互相依赖**
2. `:feature:*` 只能依赖 `:core:*`
3. 需要跨 feature 协作时 → 在 `:core:common/contract` 定义接口，提供方实现并 Hilt 绑定，调用方只注入接口

> 例：资讯页的「存为笔记」不 import `:feature:note`，而是注入 `NoteWriter`。这样删掉 `:feature:note` 时，只需换一个空实现，资讯页一行都不用改。

#### 4.7.2 首页卡片 = 插件机制

首页不硬编码任何业务卡片，只消费一个卡片注册表：

```kotlin
interface HomeCard {
    val id: String            // 唯一标识，用于持久化用户排序/显隐；一旦发布不要改
    val defaultOrder: Int
    fun isEnabled(): Flow<Boolean>
    @Composable fun Content(onNavigate: (Route) -> Unit)
}
```

- 每个 feature 通过 Hilt `@Binds @IntoSet` 把自己的 `HomeCard` 注入进来。
- `HomeViewModel` 注入的是 `Set<HomeCard>`，按配置排序渲染。
- **新增一个首页卡片 = 实现接口 + 绑定一次，首页代码一行不改，也不需要 import 你的模块。**
- 契约要求：`Content` 必须自己处理 Loading / Error / Empty 三态，不能把异常抛给首页。

#### 4.7.3 导航契约

- 路由用密封接口 `Route` 定义在 `:core:ui`，配合类型安全导航，避免字符串拼路由。
- feature 只声明自己的 `NavGraphBuilder` 扩展函数（如 `todoGraph()`）。
- `:app` 在导航图里逐个调用注册。**feature A 想跳转到 feature B 的页面时只依赖 Route 契约，不依赖 feature B 本身**。

#### 4.7.4 数据源可插拔

对「未来可能换实现」的地方全部先定义接口：

| 接口 | V1 实现 | 未来可替换为 |
|---|---|---|
| `KnowledgeSource` | 飞书公开网页（WebView 提取） | 飞书 OAuth 私有文档 / Notion / 本地 Markdown |
| `NoteWriter` | 本地 Room | 飞书云文档 / WebDAV |
| `LedgerWriter` | 本地 Room | — |
| `ReminderScheduler` | AlarmManager | — |
| `SyncProvider` | **无实现**（云备份走自己的接口，见 3.13） | WebDAV / 坚果云 / 私有服务器 |

`SyncProvider` 尤其重要：V1 虽然不做服务器，但**接口先留好**，将来接同步不需要重构数据层。

> 云备份（3.13）落地时**没有**去实现这个接口：它的 `push()/pull()` 是无参的，
> 而云备份必须携带「恢复哪一份备份」「备份密码」等信息，硬塞进这个签名只会得到一个
> 名不副实的抽象。这是「预留接口」这类设计的常见结局——真实需求出现时才知道形状对不对。
> 保留接口定义，等真的接第二个同步后端时按实际需要重定义。

#### 4.7.5 记账规则引擎可扩展

- 解析规则不写死在代码里，存本地 JSON（`parse_rule` 表 + assets 内置默认规则）。
- 规则结构：`{ package, matcher(regex), fields: {amount, merchant, direction}, priority }`。
- 支持在 App 内新增/编辑规则，也支持导入规则包 → **新增一个支付渠道无需发版**。
- 解析器按 priority 依次尝试，命中即止；未命中进未识别队列，可从原文一键生成新规则。

#### 4.7.6 配置与开关

- 所有可配置项走 DataStore，key 用 `<feature>.<name>` 命名空间，避免不同模块撞车。
- 功能开关本地化：每个 feature 可被整体关闭，关闭后其卡片、导航入口、后台任务同时失效——便于试验性功能安全落地。

#### 4.7.7 扩展性验收标准

- 新增一个「喝水打卡」类简单模块，**改动的已有文件 ≤ 3 个**（`settings.gradle.kts`、`app/build.gradle.kts`、`AppNavHost.kt`）；**首页完全不用动**。
- 删除任一 feature module 后，项目仍能编译通过。

### 4.8 骨架落地情况（已完成）

项目骨架已搭建，包含 build-logic 约定插件、6 个 core 模块、`:feature:home` 与 `:feature:todo`（插件机制的完整参考实现）。详见 `docs/开发指南.md`。

**约定插件**：一个 feature 模块的完整构建脚本只有 6 行——

```kotlin
plugins { alias(libs.plugins.myapp.android.feature) }
android { namespace = "com.myapp.feature.todo" }
```

library + Compose + Hilt + 全部 core 依赖都在 `myapp.android.feature` 插件里配好，不用复制粘贴。

**已在真机跑通**（realme GT Neo5 SE）：首页卡片流 + M2 待办完整闭环——
四个视图（今天 / 最近 7 天 / 全部 / 已完成）、新建与编辑页、右滑完成、左滑删除带撤销、重复规则自动生成下一次。
这同时验证了三件事：Room→Flow→UI 链路通、Hilt `Set<HomeCard>` 多绑定能自动发现卡片、类型安全导航的序列化器正常。
**即 4.7.2 的插件机制不是纸面设计，是跑起来的。** 后续 feature 照抄 `:feature:todo` 的目录结构即可。

**与本文档的一处刻意偏离**：4.7.1 原写「每个 feature 自带独立的 Entity/DAO」，实际实现把实体与 DAO 集中放在 `:core:database`（按 feature 分包）。原因：Room 的 `@Database` 需要编译期拿到完整实体列表，无法运行时注册；坚持原设计会让 `:core:database` 反向依赖所有 feature，依赖方向就乱了。这也是 Google 官方 Now in Android 的做法。扩展性未受损：新增 feature 时在 `model/` 与 `dao/` 下加包即可，不影响任何已有模块。

---

## 5. UI 设计规范（Claude 风格）

设计目标：**简洁而优雅**——克制的色彩、充足的留白、清晰的层级、一个温暖的强调色。不用重色块、不用高饱和渐变、不堆装饰。

### 5.1 色彩

| 角色 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| 背景 Base | `#FAF9F5`（暖米白） | `#1F1E1D` | 页面底色，**不用纯白/纯黑** |
| 卡片 Surface | `#FFFFFF` | `#262624` | 卡片、弹层 |
| 次级容器 | `#F3F1EA` | `#302F2C` | 输入框底、选中态 |
| 边框 Border | `#E8E5DC` | `#3A3937` | 1px 细描边，替代重阴影 |
| 主文字 | `#191917` | `#F5F4EF` | 标题正文 |
| 次文字 | `#6B6960` | `#A3A099` | 说明、时间戳 |
| 三级文字 | `#9A978D` | `#75726B` | 弱化信息、未选中图标 |
| 强调 Accent | `#D97757`（陶土橙） | `#E08A6C` | 按钮、进度、选中态、图表主色 |
| 成功 | `#5C8A5C` | `#7FA97F` | 完成、预算健康 |
| 警示 | `#C9A227` | `#D9B84A` | 预算 70~90% |
| 危险 | `#C0553C` | `#D4674C` | 超支、逾期、删除 |

**三条不可动摇的规则**
1. 不用纯白 `#FFFFFF` 作背景，不用纯黑 `#000000` 作深色底——一律暖调；
2. 层级靠「底色差 + 1px 描边」表达，**不靠阴影**（阴影在暖米白上会发灰、显脏）；
3. **一屏最多一个 Accent 焦点**。多个 CTA 时只有主操作用 Accent，其余用描边或纯文字按钮。

**分类色板**：记账饼图、标签使用低饱和莫兰迪色系（陶土/橄榄/雾蓝/芥末/藕荷/松绿/赭石/灰褐），与 Accent 同色温。**绝不用彩虹色**——那是廉价感的主要来源。按稳定 key 取色，保证同一分类每次颜色一致。

**深色模式是独立设计**而非简单反色，暖色调保持一致。系统动态取色（Material You）作为**可选项，默认关闭**——默认保持品牌感，这是刻意的设计决定。

### 5.2 字体与排版（已定稿）

中文字体是这套 UI 能不能立住的关键——系统默认的思源黑体在暖米白底上偏冷、字重偏轻，
和 Claude 那种「文档感」完全不是一回事。因此**必须内嵌字体，不用系统默认**。

| 用途 | 字体 | 字重 | 授权 |
|---|---|---|---|
| 标题、金额、倒数天数 | **Noto Serif SC** | Medium 500 / SemiBold 600 | SIL OFL，免费商用 |
| 正文、UI、按钮、标签 | **MiSans** | Regular 400 / Medium 500 | 小米官方免费商用 |
| 等宽数字场景 | MiSans + `tnum` | Regular | 同上 |

**为什么是这两个**
- Noto Serif SC 是中文衬线里字形最规整、字重覆盖最全的免费选择，
  且和拉丁部分（Noto Serif）同源，中英混排不会出现字重断层。
- MiSans 的字面率和字重梯度比思源黑体更接近西文无衬线的观感，
  和 Noto Serif SC 搭配时对比明确但不打架。
- 两者都是 OFL / 官方免费商用，自用项目无授权风险。

**落地要求**
- 字体文件放 `core/designsystem/src/main/res/font/`，用 `FontFamily` 显式声明，
  **禁止在 `Type.kt` 里留 `FontFamily.Serif` / `FontFamily.Default` 占位**——
  那会让 UI 在不同 ROM 上长得完全不一样（ColorOS 的默认字体和 AOSP 不同）。
- 只打包实际用到的字重。Noto Serif SC 全字重超过 20MB，
  必须先用字体子集化工具按 GB2312 + 常用标点裁剪，目标单个字重 < 3MB。
- 中文字重不要用 Bold(700) 以上——中文笔画密，700 在小字号下会糊成一团，标题用 500/600 即可。

**排版规则**
- **数字**：金额、倒数天数使用表格数字（`fontFeatureSettings = "tnum"`），否则数字变化时宽度会跳。
- 字号阶梯：`32 / 24 / 20 / 16 / 14 / 12`，行高 1.5，中文字间距约 1% 字号。
- **字间距一律用 `sp` 表达，禁止用 `em`**（v1.5 追加，源于一次真机崩溃）。
  Compose 在组件内部做样式插值时（典型场景：`OutlinedTextField` 的 label
  在聚焦时从 `bodyLarge` 过渡到 `bodySmall`）会调用 `TextStyle.lerp`，
  而它**无法在 Em 与 Sp 之间换算**，会抛 `Cannot perform operation for Em and Sp`
  并崩掉整个页面——后果是每一个带 label 的输入框都进不去。
  因此 `Typography` 的 15 个样式必须**全部显式定义**且单位统一，
  不能留给 Material 默认值（默认值用 sp，混用即触发）。规则由 `TypographyTest` 守护。
- 中英混排时西文不额外加空格，靠字体本身的边距处理。
- 段落最大宽度限制，长文阅读页两侧留白充足。

### 5.3 形状与空间

- 圆角：卡片 `16dp`，按钮/输入框 `12dp`，小标签 `8dp`，底部弹层 `28dp`。
- **阴影极轻或不用**，层级靠底色差 + 1px 描边（这是 Claude 风格的关键）。
- 间距基准 `4dp` 栅格：卡片内边距 `16~20dp`，卡片间距 `12dp`，页面左右边距 `20dp`。
- 卡片是主要容器单元，首页即卡片流。

### 5.4 组件规范

- **按钮**：主按钮实心 Accent + 白字；次按钮描边；三级纯文字。高度 `48dp`，按下轻微缩放（0.97）。
- **输入框**：无填充底色，仅 1px 描边，聚焦时描边转 Accent 并有 150ms 过渡。
- **卡片按压**：用轻微缩放（0.985）替代默认涟漪——涟漪显廉价。
- **空态**：一句简短友好的文案 + 一个主操作按钮，**绝不留白屏**。文案要具体、带一点温度，不要用「暂无数据」这种系统腔。
- **骨架屏**：所有异步卡片先显示 shimmer 骨架，不用转圈 Loading。
- **Toast/浮层**：从底部滑入的圆角浮层，带图标，2.5s 自动消失。

---

## 6. 动效设计与组件库

原则：**动画服务于反馈与层级，不做无意义炫技**。每个动画必须回答「它解释了什么」——元素从哪来、到哪去、状态怎么变。同时严守性能底线（见 6.4）。

### 6.1 动画组件库选型

| 库 | 用途 | 备注 |
|---|---|---|
| **Material 3 Expressive**（官方，首选底座） | 全局动效令牌 `MotionScheme`、形状变形、表现力组件（加载指示器、FAB 菜单） | **只取动效与形变，配色/圆角/字体沿用第 5 章自定义规范**，详见 9.2.1 |
| **Compose Animation**（官方） | 90% 的动画：`animate*AsState`、`AnimatedContent`、`AnimatedVisibility`、`updateTransition` | 基础，全部基于 spring 物理曲线 |
| **Shared Element Transition**（官方） | 列表 → 详情的元素连续过渡（卡片放大成页面） | **高级感的最大来源**，优先用官方而非第三方 |
| **Lottie Compose** | 复杂矢量插画：空态插画、完成庆祝、引导页 | 控制在 5 个以内避免包体膨胀 |
| **Haze** | 实时毛玻璃：底部导航栏、悬浮工具栏、弹层背景 | — |
| **AGSL RuntimeShader** | 高级质感：知识点卡片流体渐变、预算环形光晕、下拉刷新波纹 | minSdk 35，可直接用 |
| **Konfetti** | 达成类庆祝：完成全部今日待办、纪念日当天、月度未超支 | 克制使用，每种场景每天最多一次 |
| **Canvas 自绘 / 图表库** | 记账图表入场动画（柱状生长、折线描画、饼图展开） | 统一 600ms ease-out |

### 6.2 关键动效清单

| 场景 | 动效 |
|---|---|
| App 启动 | Splash Screen API → Logo 缩放淡出直接过渡到首页。启动图背景色必须与 Base 色一致，否则会闪一帧白屏 |
| 首页卡片入场 | 自上而下依次淡入 + 上移，间隔 40ms 形成瀑布感（**仅首次进入**，重组不重播） |
| 卡片 → 详情页 | Shared Element：卡片圆角与位置连续变形为详情页头部 |
| 底部导航切换 | 图标微弹（spring）+ 页面 fade-through（不做左右滑，竖向卡片流里左右滑显廉价） |
| FAB 长按展开 | 子按钮沿弧线依次弹出（stagger 30ms），背景 Haze 模糊 + 变暗 |
| 完成待办 | 复选框描边收拢成勾 + 文字划线 + 条目滑出列表 |
| 全部待办完成 | Konfetti 短促庆祝 + 空态文案淡入 |
| **记账成功提示** | 浮层从底部弹入（spring 带轻微过冲）→ 金额从 0 滚动到目标值 → 预算进度条 800ms 平滑推进 → 跨越 70%/90% 阈值时颜色渐变过渡 |
| 预算超支 | 进度条抖动一次（±4dp，2 个来回）+ 转危险色 |
| 每日知识点 | 卡片轻微 3D 翻转展示新内容；「已掌握」后向右滑出并缩小 |
| 经期日历 | 月份切换水平滑动，日期色块以中心为原点依次缩放淡入 |
| 下拉刷新 | Accent 色圆点形变 + 顶部液态波纹（AGSL） |
| 列表滚动 | 顶部栏随滚动收缩（标题从大号衬线缩为小号）+ 底部导航滑动隐藏/显示 |
| 长文阅读 | 顶部阅读进度条随滚动填充 |
| 深浅色切换 | 全局 300ms 颜色过渡，非瞬变 |

### 6.3 动效参数规范

统一定义在 `:core:designsystem` 的 `MotionTokens`，**禁止各处硬编码 `tween(300)`**——那会让全局手感不一致，且很难统一降级。

```kotlin
object MotionTokens {
    const val DurationMicro = 120       // 微反馈：按压、勾选
    const val DurationStandard = 280    // 常规过渡
    const val DurationEmphasized = 450  // 大位移、共享元素
    const val DurationCounter = 700     // 数值滚动
    const val StaggerDelayMs = 40       // 列表错峰

    fun <T> enterSpring() = spring<T>(dampingRatio = 0.8f, stiffness = StiffnessMediumLow)
    fun <T> exitTween() = tween<T>(180, easing = FastOutLinearInEasing)  // 退场要快
    // ...
}
```

时长基准：微反馈 `100~150ms`，常规过渡 `250~300ms`，大位移 `400~500ms`，数值滚动 `600~800ms`。**任何动画不超过 1s**。

底座接 Material 3 Expressive 的 `MotionScheme`，`MotionTokens` 只保留项目特有的补充令牌。

### 6.4 性能与可访问性底线

- 144Hz 屏**帧预算仅 6.9ms**（60Hz 是 16.6ms）；用 Macrobenchmark（**按 144Hz 配置基线**）监控，首页滚动丢帧率 < 1%。
- 优先用 `graphicsLayer` 属性动画（translation/scale/alpha），**严禁在动画中触发 layout/measure**。
- 因 minSdk 35，Haze 模糊与 AGSL 着色器**可直接全量启用，无需写降级分支**。
- 关键动效配同步触感反馈，**触感与视觉必须同帧触发**，错位会显得廉价。
- 尊重系统「移除动画」无障碍设置 → 全部动画降为瞬时。
- 设置页提供「动效强度：完整 / 精简 / 关闭」三档开关。
- Lottie 资源总计控制在 500KB 内。

---

## 7. 交付计划

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **P0（第 1 期）** | 项目骨架与模块划分、Room、**设计系统定稿**、首页 Dashboard、M1 经期&纪念日、M2 待办提醒、M3 笔记、M4 疑问 | 可日常使用的本地版 |
| **P1（第 2 期）** | M5 自动记账 + **预算与即时提示**、M9 桌面小组件、M6 飞书公开页收藏与阅读、设置页与保活自检 | 记账闭环 + 桌面入口 |
| **P2（第 3 期）** | M7 每日知识点与复习、M8 RSS 资讯、数据导出导入、云数据备份（3.13）、应用锁 | 功能完整版 |
| **P3（打磨）** | 高级动效（共享元素、Haze、AGSL、Lottie）、性能优化（Baseline Profile + Macrobenchmark）、深色模式精修、小组件多尺寸 | 体验版 |

> **设计系统必须在 P0 就定稿并落成代码**（`:core:designsystem`）。若等到 P3 再统一 UI 风格，前面所有页面都要返工。（已完成）

---

## 8. 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| **realme UI（ColorOS）杀后台**导致提醒/记账/小组件失效 | **最高** | 首启「保活自检」向导逐项引导（9.3）；提醒用精确闹钟穿透 Doze；记账保留手工补录与未识别队列；小组件走主动刷新 |
| 无服务器 → 换机丢数据 | 高 | 强提示定期导出备份；首启引导中说明；3.13 云备份作为「忘记手动导出」的兜底 |
| 云备份账号凭证明文存放（`docs/云数据备份.md`） | 高 | 实现前必须把密码移出可提交文件（本地 `.gitignore` 配置或 App 内加密存储），App 侧只落地 `access_token`，不落地密码明文（见 3.13） |
| 腾讯云开发单次请求体大小限制未知，备份快照可能超限 | 中 | V1 备份范围限定为结构化数据（不含图片），实现阶段先用真实数据量测一次实际请求体积再定是否需要分片 |
| 支付通知文案改版 → 规则失效 | 中 | 未识别队列 + 用户自定义规则；规则存 JSON 便于迭代 |
| 飞书前端改版 → 正文提取失效 | 中 | WebView 阅读始终可用，只影响摘要/搜索；提取选择器可在 App 内配置 |
| 分享链接被改为「需登录」 | 中 | 检测并在源列表标记失效，提示重新公开或替换链接 |
| 动效堆叠导致掉帧、发热 | 中 | MotionTokens 统一管控 + 动效强度三档 + Macrobenchmark 卡口 |
| WebView 无头提取耗电 | 中 | 仅 WiFi + 充电时批量刷新，限制并发与超时 |
| 数据库迁移出错 | 中 | 禁用破坏性迁移，schema 入版本控制，迁移必须有测试 |

---

## 9. 目标设备适配：realme GT Neo5 SE

本 App 单人自用，**唯一目标设备已确定**。这带来两个直接影响：可以砍掉大量兼容分支；但必须重点处理 realme UI（ColorOS 内核）的后台限制。

### 9.1 设备规格与技术基线

| 项目 | 设备情况 | 对方案的影响 |
|---|---|---|
| SoC | 骁龙 7+ Gen 2 | 性能充裕，**Haze 毛玻璃、AGSL 着色器、共享元素转场可全量开启**，不必按低端机降级 |
| 屏幕 | 6.74" 1.5K AMOLED，**144Hz** | 动画性能目标 **144fps**，帧预算仅 **6.9ms** |
| 内存 | 8GB 起 | 缓存策略可放宽 |
| 系统 | **realme UI 7.0 / Android 16（API 36）** | minSdk 35 / targetSdk 36 |
| 屏幕形态 | 直屏、居中挖孔 | 无曲面误触问题，需处理挖孔区的沉浸式布局 |

### 9.2 版本策略：minSdk 35 / targetSdk 36

设备已是 Android 16，可以把版本基线定得很高。**取 minSdk 35 而非直接 36**：API 33~35 已囊括本项目 99% 的能力收益，36 独有的只有增强触感等少数增量 API，用一行版本判断包住即可；留一点余量，将来换机或在备用机上跑不至于装不上。

**可以直接删掉的兼容代码**

| 能力 | 需要 API | 收益 |
|---|---|---|
| AGSL RuntimeShader | 33 | 液态下拉刷新、光晕**无需降级分支** |
| RenderEffect 高斯模糊 | 31 | 毛玻璃直接用，无 fallback |
| `POST_NOTIFICATIONS` 运行时权限 | 33 | 只写一套权限逻辑 |
| Photo Picker | 33 | **完全不申请存储权限** |
| `USE_EXACT_ALARM` | 33+ | 精确闹钟免用户授权 |
| 可预测返回手势 | 35 默认开启 | 直接实现 |
| 小组件预览生成 API | 35 | 小组件在选择器里显示真实预览 |
| Edge-to-edge | 35 起强制 | 只写一套沉浸式布局 |

**targetSdk 36 的强制项（必须处理，不是可选）**
- **Edge-to-edge 强制生效、无法 opt-out**。所有页面必须正确处理 `WindowInsets`，否则内容会被状态栏/导航栏遮挡。设计上本就要做全屏沉浸，方向一致。
- **前台服务必须声明类型**。`NotificationListenerService` 不属于前台服务，不受影响；将来加前台服务需补声明。
- 若引入 native 依赖，需支持 **16KB 内存页**。本项目纯 Kotlin/Java 依赖，暂不涉及。

**精确闹钟权限的简化**
核心功能为提醒/闹钟的应用可声明 `USE_EXACT_ALARM`，**安装即授予，无需引导用户手动开启**——正好覆盖待办提醒、纪念日、经期预测。相比 `SCHEDULE_EXACT_ALARM`（需跳系统设置手动开）体验好很多。
> 该权限在 Google Play 上架有严格政策限制，但本项目**不上架、仅自用**，可放心使用。仍保留 `SCHEDULE_EXACT_ALARM` 作为兜底声明。

### 9.2.1 Material 3 Expressive（直接服务于「炫酷高级动画」）

Material 3 Expressive 是本项目动效方案的最大加分项，且**它是 Compose 库能力、不依赖系统版本**，但在 Android 16 上与系统观感最一致：

- **Motion Scheme**：官方提供一整套基于弹簧物理的动效令牌（`MotionScheme.expressive()`），可直接接入 `MaterialTheme`，全局统一动效手感。作为 `MotionTokens` 的底座。
- **形状变形（Shape Morphing）**：内置多边形形状库与形变动画，用于加载指示器、按钮按压形变、进度环——比自己用 Path 手撸稳定得多。适合预算环形进度、下拉刷新。
- **表现力组件**：新的加载指示器、FAB 菜单（正好用于首页 FAB 长按展开）、工具栏动效，可省掉自研成本。

**取舍说明**：M3 Expressive 默认观感偏活泼、色彩偏鲜艳，与 Claude 的克制风格**有冲突**。方案是——**只取其「动效引擎与形变能力」，不取其默认配色与圆润外观**：色彩、圆角、字体全部沿用第 5 章自定义规范，动效层接 Expressive 的 MotionScheme。这样既拿到高级动效，又保住简洁优雅的基调。

**增强触感**：Android 16 提供更精细的振动包络 API，让关键动效配上恰当的触感反馈（完成待办的轻点、预算超支的双击警示、FAB 展开的细微反馈）。触感与动画同步是「高级感」最容易被忽略的一环。

**落地前提**：需要 `material3` **1.4.0 及以上**。首次同步项目后确认版本，到位后在 `Theme.kt` 中放开 `motionScheme = MotionScheme.expressive()`。

### 9.3 realme UI 后台限制（最高风险项）

ColorOS 系族的后台管理是国产 ROM 里最激进的之一，**会直接影响自动记账、定时提醒、小组件刷新**这三个核心能力。且**不会因为系统升级到 Android 16 而放松**——这是 OEM 层策略，别指望新系统能救。

必须在首启引导里做一个「保活设置向导」，逐项跳转并说明：

| 设置项 | 路径（realme UI） | 不设置的后果 |
|---|---|---|
| 允许自启动 | 设置 → 应用管理 → MyAPP → 耗电管理 → 允许自启动 | 重启后闹钟不重建、通知监听不恢复 |
| 允许后台活动 | 同上 | WorkManager 周期任务被杀，RSS/知识点不更新 |
| 电池优化白名单 | 设置 → 电池 → 省电与优化 → MyAPP → 不限制 | 后台被冻结，通知监听失效 |
| 通知使用权 | 设置 → 通知与状态栏 → 通知使用权 → MyAPP | 自动记账完全不工作 |
| 锁定后台任务 | 最近任务界面下拉卡片 → 锁定 | 手动清理时被误杀 |
| 关闭智能省电对本应用生效 | 设置 → 电池 | 深度睡眠时闹钟延迟 |

**引导做法**：设置页提供「保活自检」入口，逐项检测当前状态（能检测的用 API，如 `isIgnoringBatteryOptimizations`、`NotificationManagerCompat.getEnabledListenerPackages`），未通过的给一键跳转按钮 + 图文说明。首次安装强制走一遍。

#### 覆盖安装会丢通知监听绑定（2026-08-14 实测，代价最大的一个坑）

**现象**：自动记账静默失效了好几天。系统设置里「通知使用权」开关**仍然显示已开启**，
`NotificationManagerCompat.getEnabledListenerPackages` 也返回已授权，
但一条支付通知都收不到——未识别队列全空，说明通知压根没送达 App。

**定位**：`adb shell dumpsys notification` 里，服务出现在
「All notification listeners **enabled**」列表内，却**不在「Live notification listeners」列表内**。
即：系统认为用户授权了，但从来没把服务绑上。每次 `adb install -r`（或应用商店更新）
都会断开旧绑定，标准 Android 会自动重连，ColorOS 不会。

**教训**：**「已授权」推不出「能收到通知」**，这两件事必须分开检测。
只查授权状态的自检会一直显示「已通过」，而功能其实是死的——
比没有自检更糟，因为它给了错误的安全感。

**做法**：
- 进程启动时调一次 `NotificationListenerService.requestRebind(ComponentName)`——
  这就是 Android 为这个场景提供的 API，幂等，未授权时是 no-op。
- 服务在 `onListenerConnected` / `onListenerDisconnected` 里上报真实连接状态到一个单例，
  保活自检与设置页读它而不是读授权状态，「已授权但未连接」单独报未通过，
  并直接告诉用户唯一有效的手工解法：把通知使用权**关掉再打开一次**。

**兜底设计（不能只靠用户设置对）**
- 提醒类**一律用 `AlarmManager.setExactAndAllowWhileIdle`**，能穿透 Doze。
- 监听 `ACTION_BOOT_COMPLETED` + `ACTION_MY_PACKAGE_REPLACED`，重建全部闹钟与通知监听绑定。
- `NotificationListenerService` 被杀后系统通常会重启它；额外做每日自检，发现绑定丢失则通知用户。
- 自动记账**永远保留手工补录**与未识别队列，不假设自动一定成功。
- 小组件以「数据变更主动刷新」为主、周期刷新为辅，不完全依赖系统调度。

### 9.4 144Hz 高刷适配

- **帧预算 6.9ms**，比 60Hz 严苛一倍多。所有动画必须走 `graphicsLayer`，严禁在动画中触发 layout/measure。
- 动画一律用 **spring 物理曲线**而非固定帧数插值，天然适配任意刷新率。
- realme UI 默认刷新率可能是「自动选择」；如遇动画期间掉到 60Hz，可在关键页面显式请求高刷（Android 15+ 已有更完善的帧率协商机制，通常无需手动干预）。
- Macrobenchmark 的 jank 判定基线要按 **144Hz 配置**，否则按 60Hz 测出来的「无卡顿」是假的。
- 高刷 + 1.5K 下过度绘制代价明显：卡片背景、模糊层、阴影避免多层叠加，控制在 2x 以内。

### 9.5 AMOLED 与视觉

- 深色模式用**接近纯黑的暖深色**（`#1F1E1D`）而非中灰，AMOLED 上更省电、对比更好；但不用纯黑 `#000000`，避免与暖调冲突、也避免滚动时的黑边拖影。
- 挖孔屏：全屏沉浸，内容用 `WindowInsets` 避让状态栏，顶部渐隐处理。
- 小组件在深色下需保证对比度。

### 9.6 开发与调试注意

- realme UI 需在开发者选项中开启 **USB 调试** 和 **「禁止权限监控」**（否则 adb 安装的 debug 包会被反复拦截权限）。
- ColorOS 对 debug 包有额外安装拦截。**建议自己签一个固定 keystore 出 release 包日常使用**：R8 优化后才是真实性能，且系统不会区别对待。
- 环境准备与新增模块流程见 `docs/开发指南.md`。

---

## 10. 待确认项

> **当前无未决项。** 新出现的开口请追加到下面的表里并注明定稿版本。

### 已定稿（v1.5）

| 原问题 | 结论 | 落在 |
|---|---|---|
| material3 是否已含 Expressive API | **已含**。`1.4.0-alpha13` 提供 `MotionScheme`，需额外 opt-in `ExperimentalMaterial3ExpressiveApi`（与 `ExperimentalMaterial3Api` 是两个开关）。已在约定插件里全局开启，`MyAppTheme` 已接入 `MotionScheme.expressive()` 并编译验证 | 6.1 / 9.2.1 |
| 纪念日小组件默认盯哪一个 | **置顶 → 最近的下一个 → 最早创建 → 引导卡片**，四级链；置顶为单选 | 3.10 W4 |

### 已定稿（v1.4）

| 原问题 | 结论 | 落在 |
|---|---|---|
| 记账分类体系 | 内置默认一套 + **完全可自定义**（存表不写枚举，可增删改排序、停用不删除） | 3.6.1 |
| 预算周期起始日 | **默认 10 号（发薪日）**，可改为 1~28 任意一天；全局文案改用「本期」 | 3.6.2 |
| 中文字体 | **Noto Serif SC（标题/数字）+ MiSans（正文/UI）**，必须内嵌并子集化，禁止占位字体 | 5.2 |
| 小组件形态 | 四个：今日概览 4×2 / 今日支出 2×2 / 今日待办 2×2 / **纪念日倒数 2×2**；明确不做经期小组件（隐私） | 3.10 |

